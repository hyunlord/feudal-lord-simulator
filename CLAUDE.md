# CLAUDE.md — Feudal Lord Simulator

Claude Code 세션이 시작할 때 읽는 요약이다.
- 원본: 규칙은 [`AGENTS.md`](AGENTS.md)(맨 아래에서 불러온다), 헌장은 [`docs/CHARTER.md`](docs/CHARTER.md).
- 충돌 시 우선순위: [`docs/ROADMAP.html`](docs/ROADMAP.html) > 헌장 > 개별 지시서.
- 헌장을 고치면 `docs/CHARTER.md`·`AGENTS.md` 0절·이 파일을 같은 커밋에서 함께 고친다.

## 목적
잉글랜드 1300–1450 시장도시를 영주로서 키우는 등각 2D 건설·경영 게임을 **사람이 끝까지 하고 싶어 하는 게임**으로 만든다. 시스템은 재료이고, 플레이어의 경험이 결과다. 단계를 넘는 관문은 사람 플레이(G1~G4)이고, 에이전트 관문(시나리오·가드레일·결정론·깨끗한 클론)은 회귀를 막는 장치다.

## 정체성 여섯
1. **역사가 재료**
   - 대기근·전쟁·흑사병·재편이 병목을 토지 → 돈 → 사람 → 권리로 옮긴다.
   - 고증은 실제 자료로 한다. 금지 목록: 문 표식, 홉 맥주, 육각 관, 담비, 원색 염료, 현대 달력·헤어·칼라, 물레방아 곡물 방앗간, 집 굴뚝.
2. **조작 층위 셋**: 직접(건물) → 구역(붓) → 방향(권리).
3. **보이는 시뮬레이션**
   - 새 규칙은 "어떻게 보이는가"까지가 한 작업이다.
   - 위기는 세계에서 먼저 보이고, UI가 뒤따른다.
4. **압력과 위기**
   - 위기는 배운 루프의 시험이고, 예고 → 도래 → 회복으로 온다.
   - 실패는 사다리다.
5. **사람과 권리**
   - 후반은 인물·세력·권리의 게임이다.
   - 인물은 초상 풀·노화 사슬·문장으로 기억된다.

6. **모든 것이 남는다**
   - 내 결정(대안·예측·실제), 사건, 인물의 생애, 주변 세력의 역사가 전부 기록 원장에 남는다.
   - 연대기·인물 전기·세력 연대기 화면에서 "그때는 어땠나"를 글과 그때의 지도로 본다([CHRONICLE_DESIGN](docs/design/CHRONICLE_DESIGN.md)).

## 화면 원칙
- **평소엔 게임만.**
  - UI는 행동이 부를 때만 나타나고, 끝나면 접힌다.
  - 상시 정보는 날짜·계절, 인구, 식량 버팀 일수, 돈 넷뿐이다.
- **배치 가능 여부**
  - 도구를 고른 뒤 세계 위에 색 + 패턴 + 이유로 보인다.
  - 도구를 고르기 전의 안내는 튜토리얼 목표 1곳뿐이다.
- **UI 상태 기계**(평소·건설·배치·구역·선택·장부·모달·결산·일시정지)
  - 패널 슬롯은 1개다.
  - Esc는 한 단계씩 물러난다.
  - 모달이 뜨면 자동으로 멈춘다.
- **상태별 면적 예산**(평소 PC 6%·태블릿 8% 등)은 자동 측정 관문이다.
- **표기**
  - 이모지·유니코드 기호 대신 그림을 쓴다.
  - 터치 대상은 44px 이상이고, 호버로만 보이는 정보는 없다.

## 작업 방식 요약
- **작업 전**: [`docs/STATUS.md`](docs/STATUS.md)를 읽고, `docs/ROADMAP.html` 5절에서 자기 작업 ID와 `deps`를 확인한다. 그다음 지시서, 설계서, 결정 목록 순서로 읽는다.
- **지시서**
  - 형식: ID·목표·만들 것·만들지 않을 것·관문(수치)·필수 조건·시간 상한.
  - 합격은 관문으로만 판정한다. 보고 첫 줄은 관문 결과다.
  - 시간 상한을 넘으면 멈추고 보고한다.
- **질문**
  - 판정이 필요하면 물어보고, 기다리는 동안 다른 일을 한다.
  - 판정 전제가 게임 구조와 안 맞으면 지적하고 대안을 낸다.
- **무거운 검증은 DGX에서 한다**: 가드레일·전체 회귀·브라우저·캡처·성능·클론 모두 `scripts/remote/run.sh`로 돌린다(`npm run remote:*`, [docs/REMOTE_RUNS.md](docs/REMOTE_RUNS.md)).
  - 성능은 DGX 기준선끼리만 비교한다.
  - 로컬은 typecheck, 소규모 테스트, 린트까지만이다.
- **가드레일**
  - 작업당 2회까지 돌리고, 순환하지 않는다.
  - 4/5면 "알려진 정지"로 기록하고, 봇 문제는 별도 작업(BOT-n)으로 뺀다.
- **본선 `codex/phase15-organic-ground`에 합치기**
  - 순서: 작업 브랜치 → 관문 → 깨끗한 클론 → merge·push.
  - 본선이 움직였으면 양쪽을 살린다.
  - 병합 뒤 작업 브랜치(원격·로컬)는 삭제하는 것이 기본이다. 지시서가 "남겨 두라"고 명시하면 남긴다(AGENTS.md 규칙 14).
  - 본선·main 푸시는 `FLS_PUSH_OK=1 git push …` 한 명령으로만 한다. 다른 명령과 이어 붙이지 않고 `--no-verify`도 쓰지 않는다. pre-push 훅이 막는다.
  - main 병합·배포는 명시 지시가 있을 때만 한다.
- **끝나면**: `docs/ROADMAP.html`의 `TASKS`(status·note, 변경 이력 한 줄)와 `docs/STATUS.md`를 같은 커밋에서 갱신한다.
- **표기**
  - 시간은 달력 도착점("봄 말쯤")으로 쓴다. 틱·게임초는 쓰지 않는다.
  - 새 문구는 영역별 `*.ko.ts`에 넣는다.

## 세션 범위
| 세션 | 맡는 것 | 건드리지 않는 것 |
|---|---|---|
| 엔진 | 규칙·봇·저장·콘텐츠 | 렌더·UI(예외: 상태 분기 한 줄, 규칙 18) |
| 렌더 | 화면·UI·에셋 설치·입력 | 규칙·저장·콘텐츠 |
| INBOX | Astra 산출물 보관(`assets-inbox/`, 대장) | 설치 |
| REMOTE(인프라·문서) | DGX 원격 실행·플레이 서버·로드맵/헌장 문서 | 게임 코드 |
| Astra | 그림 후보 | 설치 |

## 링크
- **로드맵·상태**: [docs/ROADMAP.html](docs/ROADMAP.html)(비전·화면·시스템 지도·한 판·로드맵·작업 방식, 작업 상태는 `TASKS`), [docs/STATUS.md](docs/STATUS.md)
- **헌장·규칙**: [docs/CHARTER.md](docs/CHARTER.md), [AGENTS.md](AGENTS.md)
- **설계서**
  - [DESIGN_MASTER](docs/design/DESIGN_MASTER.md)
  - [CONTENT_DESIGN](docs/design/CONTENT_DESIGN.md)
  - [플레이어 흐름](docs/design/player-flow.md)(헌장의 PLAYER_FLOW_DESIGN)
  - [보이는 시뮬레이션](docs/design/visibility.md)(헌장의 VISIBILITY_DESIGN)
  - [기록·연대기](docs/design/CHRONICLE_DESIGN.md)
  - 결정 목록 [docs/decisions/README.md](docs/decisions/README.md)
- **조사**: [docs/research/](docs/research/)(조사 15: UI 상태·HUD)
- **운영**: [원격 실행](docs/REMOTE_RUNS.md), [플레이 서버](docs/PLAY_SERVER.md)

@AGENTS.md
