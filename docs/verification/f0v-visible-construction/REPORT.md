관문: ①건설 · ②막힘 · ③완공 · ④연기 · ⑤기표 · ⑥소리 · ⑦성능 · ⑧클론 · ⑨사용자 판정 대기

# F0-V 가시성 뼈대 1 — 건설 과정이 읽히게

- **범위:** 렌더·UI·소리·에셋·대장·테스트·스크립트·문서만 바꿨다. 엔진·구역·저장·콘텐츠·state·population은 0줄이다. 게임 규칙·자재·공사 시간·저장 형식은 그대로다.
- **명세·결정:** [가시성 명세 VC-1~VC-8](../../design/visible-construction.md), 결정 F0V-1~F0V-6, [소리 출처](../../AUDIO_LICENSES.md).
- **커밋:**
  - `f5d295d7` 1차: 기준점·bar·더미·완공
  - `dad43966` 2차: 연기·적재물·소품·기표·소리
  - `05e1349d` 3차: 막힘 탭·줌아웃 집계·완성 유령·기표 가독성
  - 이 보고서 커밋
- **F0-A:** 본선에 병합되지 않았다(`origin/claude/f0a-pressure`만 있음). 그래서 leaving/abandoned 연결(S4 보강·S12)은 하지 않았다(F0V-4).

## 관문
| 관문 | 결과 | 증빙 |
|---|---|---|
| ① 건설 | 새 게임 튜토리얼 버튼만 눌러 헛간(목재 20)을 1배속으로 관찰했다(오두막은 공짜 공사라 반입이 없다, F0V-1). 반입마다 더미가 오르고, bar 칸이 그림 단계와 함께 바뀌며, 팻말은 튀지 않는다 — 아래 관찰 절 | `observation-1x/`(10장 + 표본 81), `observation-5x/`(10장 + 표본 15) |
| ② 막힘 | 세 사유가 각각 팻말 왼쪽 양피지 탭의 아이콘과 한 줄로 뜬다. 우물은 일꾼(`일꾼 없음`), 헛간은 창고(`창고에 자재 없음`), 창고는 길(`길 끊김`)이다. 줌 0.6에서는 가까운 같은 사유 두 곳(우물·오두막)이 아이콘 하나 `×2`로 묶인다. 본선은 이모지 글자 줄이다 | `captures/blockers-{before,after}.jpg`, `blockers-far-*.jpg`, `scene/blockers.json.gz`, `tests/constructionVisibility.test.ts` |
| ③ 완공 | 1배속은 +0.3 s 먼지·지붕 크로스페이드, +0.7 s bar 가득, +1.1 s 완성 건물이다. 5배속은 먼지만 400 ms이고 bar 가득·반짝임이 없다(`COMPLETION_SEQUENCE_MS` 1200 / 400, 단위 테스트). 4 s 안의 완공은 토스트 하나로 묶인다 | `observation-1x/08~10`, `observation-5x/08~10` |
| ④ 연기 | 시장 날 도시에서 입주하고 빵이 있는 집 지붕과 가동 중 방앗간 화덕에만 연기가 난다. 빈집·빵 0 집·멈춤·미납·일꾼 0·밀 없는 방앗간은 연기가 없다(단위 테스트 2개). 본선은 연기가 없다 | `captures/smoke-town-{before,after}.jpg`, `tests/visibilitySigns.test.ts` |
| ⑤ 기표 | 조건에서만 뜬다: S1 빈 필지 말뚝(C25 구역판 필지 2곳), S2 끊긴 길 발자국(fixed12 도로 밖 헛간 2곳), S4 연기 없는 집 고리. 강조는 화면 안 최대 3개(끊긴 길 먼저)다. 조건이 없는 seed 2~4·lots24 도시는 기표 0이다. S12는 하지 않았다(F0V-4) | `captures/signs-{zoned,road,cold}-{before,after}.jpg`, `tests/visibilitySigns.test.ts` |
| ⑥ 소리 | 15개를 설치하고 라이선스를 두었다. 브라우저에서 15/15가 손으로 재생된다. 튜토리얼 40초 동안 12종이 저절로 났다: 망치 42 · 목재 부림 13 · 단계 쿵 13 · 배치 6 · 완공 4 · 알림 7 · 수레·봄 루프. 음소거 중에는 재생 0이고, 선택이 환경설정에 저장된다(`{"volume":0.7,"muted":true}`). 석재 부림·취소·불가는 이 흐름에 나오지 않아 손 재생으로만 확인했다 | `sound-probe.json`, 아래 소리 절 |
| ⑦ 성능 | p95 비 100~106 %다. lots24 끌기 첫 회는 112 %였고, 반복 두 번은 102 %·100 %였다. lots24 정지는 본선 쪽이 튀어 52 %다. C25 Node 판은 구역판 6칸만 다시 기록했다(기표 고리, 기표를 끄면 본선과 19/19 같다). 브라우저 판은 두 번 열어 6/6 같고, 본선과는 다르다(연기·기표) | `perf/`, `c25-board-browser.json` |
| ⑧ 클론 | `9320edf`의 깨끗한 클론에서 npm ci·typecheck·2,914/2,914·build 통과 | 필수 조건 절 |
| ⑨ 사용자 10분 판정 | **대기** | |

| 칸(frameWork ms, 3×240, 1280×800) | 본선 중앙 / p95 | F0-V 중앙 / p95 | p95 비 |
|---|---|---|---|
| newgame DPR 1 정지 | 2.5 / 3.4 | 2.6 / 3.6 | 106 % |
| lots24 DPR 1 정지 | 3.5 / 12.0 | 3.4 / 6.2 | 52 %(본선 튐) |
| lots24 DPR 1 끌기 | 3.5 / 5.9 · 3.6 / 6.0 · 3.6 / 6.0 | 3.9 / 6.6 · 3.7 / 6.1 · 3.7 / 6.0 | 112 % · 102 % · 100 % |
| lots24 DPR 2 정지 | 4.1 / 6.8 | 4.1 / 7.0 | 103 % |
| pop176 DPR 1 정지 | 2.6 / 4.9 | 2.8 / 5.1 | 104 % |

- **측정 뒤 바뀐 것:** 연기 시계를 벽시계에서 게임 틱으로 바꿨다(C25 브라우저 판 결정성). 그리는 양은 같다.

## ① 공사장 관찰(헛간, 1배속, 줌 1.35)
| 캡처 | 순간 | 보이는 것 |
|---|---|---|
| `01-placed` | 배치 직후(정지), 목재 4/20 | 표지(헛간 아이콘)·팻말 `목재 4/20`·bar 갈색 1/5·더미 1단, 팻말 아래 완성 헛간 유령(22 %, 희미) |
| `02-delivery-20` | 재생 시작, 4/20 | 수레꾼이 오는 중, 팻말 그대로 |
| `03-delivery-60` | 수레꾼 도착, 12/20 | 더미 2단, bar 갈색 60 % |
| `04-stage-0` | 완납 20/20 | 더미 3단, 한 줄이 도착점 `봄 중순쯤 완공`으로 바뀜 |
| `05-stage-1`·`06-stage-2` | 작업 25 %·55 % | bar 금색 1·2칸이 그림 단계와 같은 표본에서 바뀜, 먼지 퍼프, 더미가 한 단씩 줄어듦 |
| `07-stage-3` | 작업 85 % | 골조 위 지붕 단계, 한 줄 `곧 완공` |
| `08`~`10` | 완공 +0.3/0.7/1.1 s | 먼지·지붕 크로스페이드 → bar 가득(금색) → 완성 헛간 |

- **팻말 위치:** 10장 모두 같은 자리다(완성 높이 기준점).
- **수레꾼 반입:** 4 → 12 → 20이다(`observation.json` `rows`). 보인 도착점이 앞당겨지기만 하는 규칙은 단위 테스트가 확인한다.
- **5배속:** 같은 순서가 3.8 s에 끝난다. `08`에는 먼지만 있고, bar 가득·반짝임은 없다.

## 소리(15, `docs/AUDIO_LICENSES.md`)
- **명령:**
  - `place_ok`·`place_cancel`·`place_blocked`(ui)
- **알림:**
  - `alert_info`·`alert_warn`·`alert_urgent`(alert)
- **건설:**
  - `hammer_1~3`·`unload_wood`·`unload_stone`·`stage_thud`·`complete`(world)
- **환경:**
  - `cart_loop`·`spring_ambience`(world 루프)
- **출처:** Kenney Interface / Impact / RPG Audio(CC0 1.0) 14개, 이 프로젝트가 합성한 봄 환경 1개(`scripts/synthAmbience.py`)다. 라이선스 전문은 `public/licenses/audio/`에 있다.
- **설정:** 설정 창 `소리` 칸에서 음량 슬라이더와 음소거를 바꾼다(44 px 대상).

## 한 일
- **Wave 6:** 22장을 설치했다(`caBX` 없음). 대장 22행, `INBOX_LEDGER.csv` `installed_by` 22칸, 렌더 매니페스트는 출처 열거기가 읽는다.
- **공사장:**
  - 팻말: 완성 높이 기준점, 이름·4칸 bar·한 줄·막힘 탭, 줌 LOD, 겹침 올리기, 줌아웃 `×N` 집계.
  - 자재 더미 1~3단, 표지와 건물 아이콘, 완성 유령 22 %, 우물 전용 3단계.
  - 전환 크로스페이드·먼지, 1.2 s 완공과 5배속 생략, 완공 토스트 묶음, 선택 연결선.
- **시간 표기:** 벽 ETA, 빵 재고, 공사장 카드의 시간을 모두 달력 도착점(`봄 말쯤`, `내년 …`)으로 바꿨다.
- **가동:**
  - 지붕 연기: 집 그림 29장의 등성이 기준점, 입주 + 빵, 마지막 한 덩이는 옅게.
  - 방앗간 화덕 연기.
- **워커:**
  - 수레 적재물(목재·석재 더미 1단 축소, 그 밖의 짐은 줌 1.3에서 아이콘).
  - 색 사각 마커 제거.
  - 건설 워커 삽(0~1단계)·망치(2~3단계).
- **기표:** S1·S2·S4, 화면 안 최대 3 강조(잉크 받침 고리, 맥동 없음).
- **소리:** 엔진(버스 3·거리 감쇠·패닝·루프 4), 상태 읽는 감독, 첫 입력 intent에서 시작, 설정 UI.

## 발견·남은 것 → F0-V2
- **하지 않은 것:**
  - 상체 오버레이(마스크 지우고 겹치기), 운반 들보, 철거 4단계 역순 + 반환 더미(F0V-6).
  - S12 짐 진 가족, S4의 leaving/abandoned 연결(F0-A 병합 뒤, F0V-4).
- **새 게임 첫 틱:** 곡창 빵 30이 아직 집에 가지 않아 세 집에 연기 없는 집 고리가 뜬다(F0V-3). 첫 배급이 도착하면 사라진다. 첫 화면에서 끌지 판정이 필요하다.
- **기준점 수:** 지시서의 22개와 달리, 게임이 실제로 그리는 집 그림은 29장이다(F0V-5).
- **완성 유령:** 집·헛간·역사 시설 그림, 그 밖에는 스프라이트 폴백(창고 등)으로 그린다. 창고 유령은 말뚝 윤곽보다 왼쪽에 그려진다. 완성 창고 스프라이트의 등록 위치 그대로라서, 말뚝 쪽 footprint와 맞출지 판정이 필요하다(`captures/blockers-after.jpg` 왼쪽).
- **줌 LOD:** 설계서(0.6/1.0/1.35)가 아니라 기존 경계(0.8/1.2)를 쓴다(F0V-5).
- **F0-V2에 넘길 것:**
  - 생산·운반 워커 짐(밀 자루·빵 바구니·돈)의 그림 적재물.
  - 계절 연출(봄 외 환경음, 가을 수확).
  - 5배속 망치 빈도.
  - 음량 버스별 조절.

## 필수 조건
- **본선 합치기:** 본선 `32c884e8`이 이 브랜치의 조상이라 그대로 앞으로 민다(rebase·force 없음). 깨끗한 클론 `9320edf`: npm ci·typecheck·테스트 2,914/2,914·build.
- **대장:** Wave 6 22행, 출처 검사 통과.
- **문구:** 새 문구는 모두 `*.ko.ts`에 있다(`constructionPlaqueCopy`·`calendarArrivalCopy`·`completionToastCopy`·`audioCopy`).
- **입력:** 소리 시작은 intent bus 구독자다(App에 DOM 리스너 없음, `inputIntentBoundary` 통과).

## 소요 시간
- 2026-09-26 01:47(INSTALL-5c 본선 커밋) → 09:14(KST, 벽시계) = 7시간 27분이다. 8시간 상한 안이다. 1차(기준점·bar·더미·완공), 2차(연기·소품·기표·소리), 3차(막힘 가독성·집계·유령·증빙)로 나눴다. 벽시계에는 문맥 요약으로 멈춘 시간이 들어 있다.
