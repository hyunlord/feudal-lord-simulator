# R1-fix 요구사항 정합 검토

판정: **코드/시험 항목 대응 PASS(아래 한계 포함), 전체 관문 완료 FAIL — 최종 실행 증거 대기**.

검토 기준은 `/Users/rexxa/github/feudal-lord-simulator-playable`의 `1c09e99..d3d391c`와 검토 시점 R9 미커밋 변경이다. AGENTS, WORK_ORDER, 관련 S명세·결정 기록·구현·시험을 읽었다. 읽기 전용 검토이며 CPU 경쟁을 피하려고 테스트/성장/성능 재생을 실행하지 않았다. 따라서 테스트 파일 존재나 이전 보고값을 현재 마지막 커밋의 통과로 판정하지 않는다.

## 확정 코드 차단 결함

이번 요구사항 대조에서 새로 확정한 코드 차단 결함은 없다. 아래 실행·통합 증거 없이는 완료할 수 없다.

1. **R-T15 통합 증거 공백**: `tests/autoplayWheatTransportRecovery.test.ts:32-40`은 24,000틱 동안 `foodAction`만 실행한다. 실제 `decideNextAction`의 앞선 우선순위와 R9 예산은 통과하지 않는다(`src/engine/autoplay.ts:225-259`). 따라서 이 시험은 자연 seed1 원본에서 식량 하위 조언의 회복성을 증명하지만 전체 봇 회복성을 단독 증명하지 않는다. 최종 검증에서 같은 fixture에 전체 `decideNextAction`을 사용한 재생으로 N틱 내 빈집 0 및 최종 빈집 0을 확인하거나, 동일 상태를 포함하는 통합 증거를 제시해야 한다. 코드 고장이라고 단정한 항목은 아니다.
2. **최종 성능 증거**: R-T16 단위 시험은 1초를 검사하지 않는다고 명시한다(`tests/autoplaySearchBudget.test.ts:18-19`). `scripts/autoplaySearchReplay.ts:37-53`는 원본 SHA와 69개 직렬 사례, seed3-28080 <1초/나머지 <10초, 동일 입력 재호출 결정론을 실제로 판정한다. 마지막 제품 커밋에서 이 실행이 69/69여야 한다. 현재 R9는 미커밋이므로 완료 주장은 불가하다.
3. **최종 필수 관문**: R-T1~17 전체, 기준선 조건의 5개 seed 각각 L4 24/24·안정 24,000틱·시설 상한·경고 <10%, 원본 탐침 7개 비재현, 깨끗한 클론 전체 회귀/형검사/빌드가 필요하다(`WORK_ORDER.md:8-10,60-76`). 독립 탐침은 저장소 내부에 이식되어 있으나 이 보고서는 실행값을 확인한 것이 아니다. seed당 25분 초과는 미판정이다.
4. **완료 문서**: `docs/STATUS.md:21`은 아직 R1-fix를 후속 작업으로 설명한다. 최종 결과·해시·판정으로 갱신해야 한다. 현재 진행 중인 상태에서 이 누락을 코드 결함으로 세지는 않는다.

## 지정 쟁점 판단

- **R3 인력 순서 — 최소 조정으로 수용**: `labour.ts:94-101,181-185`는 공사 최소분을 먼저 떼고 핵심 식량 한 사슬, 핵심 목재 한 쌍, 중복 식량 순이다. 원문 ‘기존 우선순위’의 문자 그대로와 차이가 있지만 R-T5의 추가 농장 2개+주택 공사+벌목/제재 유지 요구는 기존 모든 식량 우선과 16명 풀에서 동시에 성립하지 않는다. `tests/buildingOperation.test.ts:12-18`의 3+8+5명 보존, `S5-labour.md:11`와 `docs/decisions/README.md:83`에 이유가 명시되므로 새 경제 구조로 판정하지 않는다. 최종 보고에서 이 예외를 숨기지 말아야 한다.
- **R5 공유 용량 — 기존 한도 보존으로 수용**: `storage.ts:140-145`는 모든 품목의 물리적 공유 한도다. 취소 반입만 우회한다(`deliveryReturn.ts:158-163`), 가까운 경로 있는 창고를 고른다(`constructionReturnDestination.ts:19-36`). 이미 공유 한도가 가득 찼으면 다른 품목도 입고할 공간이 없으며 이는 별도의 공급원 제외 상태가 아니다. 소비 후 용량이 생기면 보통 입고 계산이 다시 열린다. `S6-cancellation.md:5-6`, 결정 목록:84에 해석이 기록되어 있다. 별도 품목별 용량제를 새로 요구하지 않는다. R-T10/11은 실제 반입/다음 공사 재사용 및 파생 표시 제거를 다룬다.
- **R7 회복 — 구조상 요구 대응**: `autoplayTimberRecovery.ts:11-47`은 2,400틱 연속 부족, 진행 중 증설 초기화, 인력·저장·자재 제한을 사용한다. `tests/autoplayTimberScenario.test.ts:14-32`는 기본 새 게임의 전체 조언을 120,000틱 실행해 증설과 이후 목책 진척을 검사한다. 이 시나리오는 충분한 모양이다. 최종 R9 포함 결과는 별도 재확인이 필요하다.
- **R8 회복 — 하위 시나리오 적합, 전체 조언 통합은 위 공백**: `autoplayFood.ts:45-60`은 기존 도로/곡창 조치 후 수요 근처 방앗간을 시도하고 진단에 선택을 기록한다. `autoplayWheatTransportRecovery.ts:24-48`은 실측 창, 시설 상한, 노동력, 자재, 기존 빈 방앗간 억제, 실제 공급/수요 경로와 서비스 공간을 검사한다. 정확한 지속 시간의 구현은 ‘진단이 연속으로 유지된 타이머’가 아니라 완전한 2,400틱 관측창의 원료 부족 경험과 현재 진단이다. S8 명세:14에 명시된 해석이며, 엄격한 연속 진단으로 과장하면 안 된다.
- **R17 원본 부족 — 허용된 대체임을 명시**: `stoneConstructionFlow.test.ts:57-64`의 16구간은 준비 상태이며 원본 seed2가 아니다. 지시서가 fixture를 ‘가능하면’으로 명시했고 S6-cancellation:11도 한계를 드러낸다. R9 전체 통합과 별개로 자연 seed2 16구간 재현을 했다고 보고하면 안 된다.

## 나머지 대응 및 저장 절차

R-T1/2 서비스 선착순, R-T3/4 성벽 인력, R-T5/6 가동/공사, R-T7 시설 중지 원인, R-T8/9/17 석벽, R-T10/11 화물, R-T12/13 판매 유보, R-T14 목재, R-T15 밀 운송, R-T16 예산의 이름 있는 시험이 모두 존재한다. 각 결과 수는 실제 실행에서 집계해야 한다.

`SAVE_SCHEMA_VERSION=4`(`src/save/saveTypes.ts:5`), v2→v3와 v3→v4 등록(`src/save/migrations/index.ts:18-19`), 구버전 의미를 보존하는 선택 필드 마이그레이션, v3/v4 fixture가 있다. 지문에는 `operationPaused`와 `expansionShortageSinceTick`가 포함된다(`src/save/schemaFingerprint.v4.json:225,350`). `scripts/saveSchemaFingerprint.ts:23-24`에서 실제 해당 상태 fixture를 읽고 스키마 감시 시험은 유지된다. 최종 깨끗한 클론 실행으로 PASS를 확정해야 한다.

수정 파일: 이 검토 보고서만. 제품 변경·무거운 테스트·하위 에이전트 실행 없음.
