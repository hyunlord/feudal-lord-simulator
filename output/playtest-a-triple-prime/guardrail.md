# A‴ 자동 성장 가드레일 영수증

시뮬레이션 원본은 `9b5b51c`(clean source)이다. seed 1~5 각각 24필지, 최대 1,200,000틱을 실행했다. 준비된 개시 상태를 지도별 합법 좌표로 옮긴 검증 fixture이며 자연 화면 플레이가 아니다. `final.jpg`는 제품 렌더러의 1600×1100, zoom 0.9 캡처다.

| seed | 종료 틱 | L4 | 승리 틱 | 밀 0 방앗간 | 7db 기준 | 가드레일 | 캡처/수락 상태 |
|---|---:|---:|---:|---:|---:|---|---|
| 1 | 679,251 | 24/24 | 630,336 | 2/9 (22.2%) | 0.0% | 실패 | 성공; 판정 `acceptance-unmet` |
| 2 | 429,362 | 24/24 | 383,280 | 2/10 (20.0%) | 0.0% | 통과 | 성공; 판정 `passed` |
| 3 | 414,592 | 24/24 | 307,332 | 0/8 (0.0%) | 0.0% | 통과 | 성공; 판정 `passed` |
| 4 | 540,864 | 24/24 | 466,429 | 0/9 (0.0%) | 0.0% | 통과 | 성공; 판정 `passed` |
| 5 | 603,536 | 24/24 | 542,560 | 3/9 (33.3%) | 0.0% | 실패 | 성공; 판정 `acceptance-unmet` |

**가드레일 3/5 통과.** 지정 기준선은 `7db9df85`, 밀 0 방앗간 비율의 허용 악화는 +20%p다. 모든 seed가 L4 24/24, 승리, 24,000틱 안정, 전체 서비스를 달성했다. 수락 상태 `acceptance-unmet`은 촬영 실패가 아니라 가드레일 미달을 뜻한다.

`89ce592`는 `9b5b51c` 이후 온보딩 UI와 대응 테스트 네 파일만 수정했다(`git diff --name-only 9b5b51c..89ce592`). 엔진 코드는 그 차이에서 바뀌지 않았다. 따라서 이 재실행은 엔진 결과의 최신 범위를 확인하지만, 캡처를 `89ce592`의 정확한 화면으로 부르지 않는다.

실패 seed 1·5의 밀 0 비율은 기준선보다 20%p 초과 악화됐다. 두 최종 상태의 SHA는 A″ `689bda7` 및 이전 A‴ `c093d2f`와 각각 동일하다. 따라서 이 실패는 이번 A‴ 변경이 새로 만든 현상은 아니지만, 지정된 `7db9df85` 기준에서는 여전히 실패다. 기준선·허용치를 바꾸거나 실패를 통과로 처리하지 않았다.

유휴 일꾼 비율은 31.4~32.6%이며 이번 판정값이 아니다. 5~25% 목표는 C3 후보로 남긴다. 원자료는 `seeds/final-9b5b51c/seedN/summary.json`, `acceptance.json`, `final-state.json`, `final.jpg`에 보존했다.
