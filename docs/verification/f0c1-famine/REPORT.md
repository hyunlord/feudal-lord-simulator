관문: ①C 10/10 · ②1장 완주 5/5 · ③대응 비교 이탈 0 대 72·금고 5/5 적음 · ④결정론·저장 v14 · ⑤클론 `70140eb` 2,991/2,991 · 가드레일 4/5(3회차, seed 3 EV8)

# F0-C1 흐름 뼈대 3 — 대기근·첫 청원·연대기·1장 끝 보고서

지시서: F0-C1(대기근 준비도 게이트·첫 청원·연대기 데이터·1장 끝). 명세: [1장 명세](../../design/flow-chapter-one.md) FC-1~FC-7. 결정: FC1~FC7. 엔진 세션, 렌더 0줄. F0-B 병합 뒤 본선(`a205a477`)을 합쳐 이어 했다.

## 커밋

| 커밋 | 내용 |
|---|---|
| `41960516` | 대기근(시대 사건, 준비도, 수확 반 2~3번, 값 × 3, 1312 예고), 대응 4종, 시장권 청원, 권리, 연대기·장 끝, 저장 v14, C1~C10 |
| `64fec5b8` | 값 충격(가난한 1/4, 구휼이 그 빵값을 냄), 청원은 필지 6개 마을, 결정 FC1~FC7, 명세 |
| `eaaa4987` | 본선(F0-B 병합, INSTALL-11, INBOX-1g/h) 합침, AR-9 되돌림 유지 |
| `3ed325df` | E10이 현재 스키마(v14)를 읽게 — 가드레일 1회차 |
| `136f6f06` | 구휼 예산 = 지난 계절 현금 수입까지(1회차 seed 5: 금고 5,274 → 268) |
| `a939d8a5` | 봇 석재 창고 복구 확장(채석장이 없고 원석 칸이 통나무로 가득이면 창고, FC8) — 가드레일 2회차 |
| `f20177ea` | 봇 복구 `log_overflow`(AR-10, 통나무 넘침이면 제재소 먼저, FC9) |
| `fe3c0657` | 대기근은 1320까지 들어온 시대에서만(옛 저장이 불러올 때 시대가 한꺼번에 들어오는 경우 제외), v14 지문·픽스처, 고정값 — **관문 ②③·가드레일 3회차 기준 커밋** |
| `5a98e377` | v0 승격 테스트 v14까지, 결정 FC9·FC10 |

## 관문 결과

### ① 시나리오 C1~C10 — 10/10 (`tests/flowChapterOne.test.ts`, 테스트 9개: C5·C6은 한 테스트)
C1 소문 1312·징후 1313(젖은 여름 1313·14, 값 × 1.2)·준비도 대기·1320 강제·준비되면 1315 도래 · C2 수확 반 2~3번, 값 × 3(빵 15), 다음 좋은 수확에 끝, 시대 효과 관 · C3 구휼: 가난한 가구 빵을 기근 값으로 곡창에(`famine_relief`, 출처 사건), 금고 절반 이하, 이탈 1/계절, 값 충격에서 가난한 1/4만 떠날 준비·구휼이면 없음 · C4 가격 통제: 값 × 1.5, 게이지 −10/계절, 가난한 가구도 삼 · C5 방관: 변화 없음 · C6 투기: 곡창 1/4 판매(`famine_sale`), 이탈 3/계절 · C7 청원 1305–1308, 필지 5개 마을엔 없음, 1308 뒤 만료(−10) · C8 수락(권리·좌판세 × 0.75·출처 권리·+15)·가격 붙여(+150 `charter_fee`·+5)·거절(−20) · C9 연대기 한 쪽(결정 3개 무게순, 사건·통계) · C10 장 끝(시장도시 ∧ 60 %), 60 % 미만·촌락은 아님, 저장 v14 왕복, v13 승격(기근 시대에 든 저장은 대기근 놓침).

### ② 표준 봇 1장 완주 — 5/5 (`scripts/chapterRun.ts`, 100,000틱)

| seed | 대기근 진입 | 반 수확 | 도래 인구 → 끝 인구 | 청원 | 1장 끝 |
|---|---|---|---|---|---|
| 1 | 1320 (강제) | 2 | 528 → 528 | 1306 수락 | 1322 |
| 2 | 1316 | 3 | 473 → 463 | 1307 수락 | 1320 |
| 3 | 1318 | 3 | 214 → 301 | 1306 수락 | 1321 |
| 4 | 1317 | 2 | 515 → 528 | 1306 수락 | 1320 |
| 5 | 1320 (강제) | 3 | 528 → 528 | 1307 수락 | 1323 |

- 두 seed는 시장이 1320 가까이 서서 준비도가 늦어 1320에 강제로 들어왔다. 1장 끝은 대기근이 끝난 뒤라 1320~1323(흐름 설계서의 1318보다 늦다). `fe3c0657` 기준(`a939d8a5`와 같은 수치).
- 증빙 [gate2/](gate2/) · [summary.json](gate2/summary.json).

### ③ 구휼 대 방관 — 구휼이 이탈 적고 금고 적음 5/5

| seed | 구휼: 대기근 이탈 · 구휼 지출 · 끝 금고 | 방관: 대기근 이탈 · 끝 금고 |
|---|---|---|
| 1 | 0 · 885 · 4,779 | 12 · 5,599 |
| 2 | 0 · 1,305 · 3,284 | 19 · 4,587 |
| 3 | 0 · 870 · 3,564 | 9 · 4,507 |
| 4 | 0 · 1,095 · 4,587 | 12 · 5,579 |
| 5 | 0 · 1,455 · 5,167 | 20 · 6,496 |

- 이탈 = 대기근 기록의 손실(흉년 수확 + 2계절부터 회복 끝까지 2단 이탈). 방관도 1장은 끝난다(끝 인구 ≥ 60 %).
- 구휼 예산은 지난 계절 현금 수입까지다(FC3). 처음(금고 50 %)에는 가드레일 1회차 seed 5에서 금고가 바닥나 채석장·교회를 끝내 못 지었다.
- 첫 탐침에서는 두 봇이 똑같았다: 대비한 봇의 24필지 도시는 비축이 6~11계절분이라 수확 −50 %만으로 이탈이 0이었다. 흐름 설계서의 "가격 3배 → 아사·이탈"을 값 충격(FC3)으로 넣어 차이가 생겼다.

### ④ 결정론·저장 v14 — 통과
C10(저장 v14 왕복 뒤 300틱 같음), v13 → v14 승격(픽스처 10종 `fixtures/saves/v14/`, 지문 `schemaFingerprint.v14.json`), E10(v12 승격)도 현재 스키마로 통과.

### 가드레일(필수 조건: 엔진 규칙 변경) — 3회차 4/5 (DGX)

| seed | 1회차 `3ed325df` | 2회차 `a939d8a5` | 3회차 `fe3c0657` |
|---|---|---|---|
| 1 | 통과 166,080 | 통과 153,976 | 통과 153,976 |
| 2 | 통과 203,930 | 통과 198,810 | 통과 198,810 |
| 3 | 회귀 14필지 | 정지 18필지 | 정지 18필지(96,000~168,000틱 조치 없음) |
| 4 | 통과 155,843 | 통과 151,101 | 통과 151,101 |
| 5 | 회귀(교회 없음, L3) | 통과 364,601 | 통과 300,416 |

- seed 3: F0-B와 같은 1310년 작은 성벽(성 안 132칸) — 결정 EV8 "알려진 정지". 이번 경로에선 18필지까지 가고 곡창 공백 자리도 없다.
- seed 5 1회차: 구휼이 계절마다 금고 절반을 써 5,274 → 268로 바닥났고, 그사이 경로가 바뀌어 창고 두 채가 통나무로 찼다 → 채석장이 "받을 곳 없음"으로 안 서고 → 석재 0 → 교회 없음 → L3에서 멈춤(금고가 16,018로 돌아온 뒤에도). 고침: 구휼 예산을 지난 계절 수입까지(FC3), 석재 창고 복구 확장(FC8), 통나무 넘침이면 제재소 먼저(FC9, AR-10). 2회차에서는 FC8만으로 늦게 회복했다(작업 중엔 300,000틱에서 회귀로 잘못 판단했고, 3회차는 사용자 허락으로 돌렸다).
- 사용자 판정: 3회차가 마지막, 4/5(seed 3 EV8)면 병합. 두 정지(작은 성벽·통나무 넘침)는 BOT-2 범위.
- 증빙 [guardrails.json](guardrails.json).

### ⑤ 깨끗한 클론 — 통과
DGX `remote:clone-check` `70140eb`: 새 클론(+LFS) → `npm ci`, typecheck, 전체 회귀 2,991/2,991, build.

## 필수 조건
- 전체 회귀·클론: DGX(`scripts/remote/run.sh`, REMOTE-1) — 깨끗한 클론 `70140eb` 2,991/2,991. 첫 클론 검증(`7c173ba`)에서 11건이 깨져(옛 픽스처가 불러오며 기근 시대에 바로 들어가 대기근이 1330·1600년에 도래, v14 지문, 고정값) `fe3c0657`·`5a98e377`에서 고쳤다.
- 렌더 0줄. 봇: 대기근 대응·청원 응답 행동, 흉년 대비 여유 상한, 석재 창고 복구 확장(FC8). 새 장부 분류 `famine_relief`·`famine_sale`·`charter_fee`(문구 `ledgerCopy.ko.ts`), 봇 문구 2줄(`autoplayCopy.ko.ts`).

## 렌더 세션이 넘겨받을 것
- 대기근: `famineStatus(state)`(단계·고른 대응·남은 선택지·끝 틱), 예고는 `eventForecast`(소문 1312·징후 1313~).
- 대응 선택지: `famineResponse(state, choice)` / 게임 행동 `famine_response`.
- 청원: `openPetitions(state)`, `state.politics.petitions`, 응답 `respondToPetition` / 행동 `petition_response`.
- 권리: `state.politics.rights`, `rightsEffectRegistry`; 상인 게이지 `state.politics.merchantGauge`.
- 연대기·장 끝: `chapterEnd(state)` → `ChronicleEntry`(사건·결정 3개·통계).

## 다음 후보
- 대기근 진입이 세 seed에서 1320 강제: 봇이 시장을 늦게 짓는다(시장 개설은 흐름 설계서 30–45분). 1장 끝도 1320~1323으로 늦다.
- 가축 역병은 데이터만(가축 없음, FC2).
- 가격 통제·투기의 상인 게이지는 아직 다른 규칙이 읽지 않는다(권리 체계 E).
- 창고 "받는 품목 토글"(UX-3R)이 없어 통나무 넘침을 플레이어가 직접 풀 수 없다(FC10 메모).

## 소요 시간
F0-C1: 11:50 무렵 브랜치 시작(F0-B 가드레일과 겹쳐 진행) → 14:30 병합 준비(약 2시간 40분).
