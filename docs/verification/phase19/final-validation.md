## Phase 19 — 읽기 쉬운 UI·실제 CPU 작업량·도시 성장 검증
- 커밋: 기준 `2a919d7`; 정책 `e9b6cec`, 성장 검증 `1494e0c`, 계측 `687072c`, UI `591efdf`·`304d575`, 최종 회귀 보정 `398174c` (6파일). 증빙은 별도 커밋한다.
- 바뀐 것: UI 가독성·증명 모드 CPU 계측·검증용 성장 한도·검수 정책을 각 담당 작업으로 분리해 통합한다.
- 규모·측정: 이 Mac·Chrome 개발판의 **16주택·264명·비주거시설29개** 도시에서 DPR2·1× 진행 정지 시 frameWorkMs 중앙값/p95 **28.60/30.60ms**, tickWorkMs **0.40/0.70ms**. 과거 A/C와 현 도시 총24조건·108측정 창을 완료했다. 주택 래스터 효과는 회차 변동이 커 안정적 개선을 입증하지 못했고, 도로 라벨의 8주택 전체 틱 중앙값은 동일했다. draw CPU는 GPU 완료·React 전체 비용이 아니며 rAF는 보조다. 상세는 B 보고서 참조.
- 테스트: 최종 `398174c`에서 typecheck / **전체 1,544/1,544 (Phase9 포함, 실패·취소·건너뜀0)** / build / checkContrast 통과. 전체 테스트508.95초. 앞선 `304d575`의 1,539/1,543 실패는 옛 10/11/13px 직접값 기대를 13px 토큰·실제 값 검사로 갱신하고, 초기 참조 생성을 기존 소유 모듈로 이동해 LOC 제한을 유지하여 보정했다. 렌더 계측 경계는 그대로다.
- 실제 화면: 6조건 UI·제한된 도로/성문/시장 QA 통과. 최종 소스 실제 Chrome에서 일반 모드 계측 비활성, 54틱=54표본, 누적250프레임 중 최근240 보관, 해제·재마운트 초기화 확인. CSS 불변이므로 기존 6조건 검수를 재사용했다. 코드·보안 독립 검수 통과. 이 결과는 자연 성장 C의 미충족 기준을 대신하지 않는다.
- 확인 못 한 것: 상한 16/32/48 실험 모두 60만 틱 후 실제 16주택·264명·목책 시대에 머물렀다. 최종 석재 241/400, 주택 미충원·식량 지원 한도 조건이 관찰됐으며 후기 시대 확장 분기가 이번 정체의 원인은 아니다. 자연 수용량 초과 사례는 0개여서 C 완료 기준은 미충족이다. 제어한 스트레스 상태로 자연 성장 통과를 대신하지 않는다.
- 사용자 결정 필요: E 기본 성장 한도·후기 확장 정책, D 선택 한도5개 시드, G 아트 적용은 승인 전 구현하지 않는다.

빌드의 JS598.06kB 권장치 경고는 남는다. 별도 lint/LSP 실행기는 구성되지 않았으며 tsc·렌더 소스 가드·대비 검사로 확인했다. 명령·로그는 final-verification.json, 측정 재현법은 B-reproduce.md에 기록한다. **Phase19 전체 완료는 아니다.** C 미충족과 E/D/G 승인 대기를 유지한다. 증빙3MB 상한을 전달 전에 확인하며 main 병합·배포·이력 재작성·LFS 정책 변경은 하지 않는다.
