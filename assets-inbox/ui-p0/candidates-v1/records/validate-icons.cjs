const fs=require('fs'),{createCanvas,loadImage}=require('/tmp/astra-wave4b-work-20260925/node_modules/@napi-rs/canvas');(async()=>{const root=require('path').resolve(__dirname,'..')+'/',rs=JSON.parse(fs.readFileSync(root+'records/icons.json')),checks=[];for(const r of rs){const im=await loadImage(root+r.file),c=createCanvas(im.width,im.height),x=c.getContext('2d');x.drawImage(im,0,0);const d=x.getImageData(0,0,c.width,c.height).data;let z=0,o=0,max=0;for(let i=3;i<d.length;i+=4){if(d[i]===0)z++;if(d[i]>=250)o++;max=Math.max(max,d[i])}if(im.width!==r.width||im.height!==r.height||!z||!o)throw Error(r.id);checks.push({id:r.id,width:im.width,height:im.height,transparentPixels:z,nearOpaquePixels:o,maximumAlpha:max,hotspot:r.hotspot||null})}fs.writeFileSync(root+'records/icons-validation.json',JSON.stringify({assets:rs.length,sheetCells:rs.filter(x=>x.cellSize).reduce((n,x)=>n+x.cellOrder.length,0),alphaNote:'Generated native alpha preserved; resampling may leave max253/254 rather than255. No whitening or flattening.',checks},null,2));console.log('17 dimensions/native alpha checks passed;57cells')})();
