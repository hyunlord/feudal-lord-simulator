## Phase 16 — E 카메라 이동 성벽 캐시
- 커밋: 7ba79a0 (4파일).
- 바뀐 것: 완성 성벽 단위 캐시에서 이동·뷰포트를 제외한다. 정확한 줌/DPR과8M픽셀 상한을 유지하고 이동 위치만 정수 장치픽셀로 맞춘다.
- 측정: Apple M4 Max / Darwin25.4.0 / Chrome153.0.8010.48 헤드리스,1600×1100, 인구176 동일 저장 도시·카메라. 40프레임×4회 중 첫 회 제외, 단위ms.

| DPR·조건 | 중앙값 전→후 | p95 전→후 |
| --- | --- | --- |
| 1·일시정지 | 49.9→49.9 | 66.7→66.6 |
| 1·1× 정지카메라 | 50.0→50.0 | 66.7→66.7 |
| 1·1× 200px/s 이동 | 66.6→50.0 | 66.7→66.7 |
| 2·일시정지 | 83.4→83.3 | 116.7→83.4 |
| 2·1× 정지카메라 | 83.4→83.3 | 100.1→100.0 |
| 2·1× 200px/s 이동 | 83.4→83.3 | 100.1→100.0 |

- 테스트: 최종1512/1512·타입·빌드 통과. 36캐시 조합 최초/반복/화면 밖 복귀 차이0. 드래그/진행정지 비율 두DPR 모두1.00으로1.5이내 기준 충족. 측정 중 캐시 miss·퇴거0, 보유 최대869,680픽셀. 최종 통합 진행/이동 중앙값 DPR1 50.0ms, DPR2 83.3ms이며60FPS 결과가 아니다.
- 확인 못 한 것: DPR2 0.1ms 차이는 실질 속도 향상으로 해석하지 않는다. 직접 그리기와 캐시는 소수점 이동 시 가장자리 안티앨리어싱이 달라 완전 픽셀 동일하지 않다.
- 사용자 결정 필요: 없음. 다른 장면·하드웨어·배포 성능으로 일반화하지 않는다.

재현: `PLAYWRIGHT_MODULE=<기존 playwright-core 경로> node scripts/phase16Benchmark.mjs --task E --variant before --dpr 1 --condition paused --url <동일 소스 서버> --source-root <해당 작업트리> --output <결과 디렉터리>`; after/DPR2/running/drag도 동일 조건. 저장 도시 `tests/fixtures/phase16-city.json.gz`(해제 SHA183c1fa74b4cac58d97e73eb431df1ed534ccad8fe490fbd554f86aa89e64732).
