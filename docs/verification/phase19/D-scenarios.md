## Phase 19 — D 제한된 상호작용 회귀 검증
- 커밋: `304d575` 통합 소스. 이 검증 자체의 제품 변경 없음.
- 바뀐 것: 없음. 실제 UI와 현재 진단 모델로 세 가지 동작을 확인했다.
- 측정: 일반 시작 상태에서 주택L3(tick201) → 도로 단절 후L1(tick2051) → 복구·새 배급 후L3(tick2841, 마지막 배급2791). 건축 외형 유지, 상태 오버레이 표시·해제 확인.
- 테스트: 제어한 성벽/시장 배치에서 실제 성문 도로 철거·복구 시 served→unreachable→served. 별도 일시정지 상태의 시장 일꾼3명은served,2명은understaffed이며 상세창 이유가 일치했다. 브라우저 오류0.
- 확인 못 한 것: 성문 자연 건설이나 UI 인력 변경·자연 인력 회복을 시험한 것은 아니다. 시장2/3명은 각각 별도로 구성한 상태다.
- 사용자 결정 필요: E의 기본 규모/확장 정책 결정 후 선택 규모의5개 시드를 검증한다. 이번 세 시나리오로 대규모 자연 검증을 대체하지 않는다.

상세 수치는 D-qa-summary.json에 기록했다. 추가 캡처는 중복 용량을 줄이기 위해 무시된 output/phase19/qa에만 보존했다.
