## Phase 17 — 실제 UI·렌더 검증
- 커밋: `d08f572` (최종 제품 소스6파일).
- 바뀐 것: 새 지형 채움과 성벽 재질을 실제 게임 UI·동일 렌더러 검사판에서 검수했다. 검사판은 합성 배치이며 자연 플레이로 설명하지 않는다.
- 측정: 지형36프레임·36알파 및 추가48알파 조합 통과. 최대 색 채널차14/평균0.12093, 불투명 내부 최대1. 가장자리 알파차 최대60은2픽셀 경계 안에 한정되며 내부 구멍0. 픽셀 완전 동일을 주장하지 않는다.
- 테스트: 실제 UI4조건(DPR1/2·줌1/1.35)에서 길 설치·철거,120px 카메라 이동, 휠 확대 통과. 초록·숲·암석·물, 음수 투영X, 인접3×3 이음새 확인. 성벽 캐시36조합의 최초/반복·화면 밖 이동 후 복원 차이0, 구조적 실루엣 오류0.
- 확인 못 한 것: **기본 GPU의240조합 엄격 픽셀 안정성은 미충족**이다. 구버전과 후보 모두 반복 readback 중 도로 픽셀이 변했다. 실제 화면에서 새 도로 소실은 관찰하지 못했지만 원인을 제품 수정으로 해결했다고 주장하지 않는다.
- 사용자 결정 필요: 없음. 상세 수치는 qa-summary.json·browser-summary.json에 있다.

가설3개를 구분했다. 실패 순간 저장 스택 깊이0·clip0·alpha1·합성source-over·변환 동일, 도로 fill13회가 매번 실행되어 상태 누수와 도로 그리기 생략은 지지되지 않았다. 성벽·호버 없이 동일한 도로+집20회 출력도 양쪽 버전에서 같은 초기 알파 변화가 있었다. 모든 캔버스를 CPU readback 모드로 고정한 별도 진단에서 기준240/후보240 및 각3회 반복은 모두0차이였으나, **이 결과는 기본 GPU 통과를 대신하지 않는다**. 제품의 캔버스 설정은 변경하지 않았다.
