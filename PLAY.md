# 영주 시뮬레이터 실행

Node.js **22 이상**(22.x는 22.12 이상, 권장 24 LTS)과 npm이 필요합니다. Vite 8.2.0과 테스트의 전역 `WebSocket` 기준입니다.
Chrome, 화면 **1600×1000 이상**을 권장합니다.

프로젝트 폴더에서 처음 한 번 설치한 뒤 실행하세요.

```sh
npm ci
npm run play
```

배포용 빌드가 끝나면 브라우저를 자동으로 엽니다. 열리지 않으면
**http://127.0.0.1:4173/** 에 접속하세요. 포트가 사용 중이면 다른 포트로 넘어가지 않고 종료합니다.
실행 터미널은 켜 두고, 서버 종료는 `Ctrl+C`입니다.

## 조작

- 첫 안내는 아무 곳이나 클릭해 닫습니다. 아래 메뉴에서 건물을 골라 지도 클릭으로 설치합니다.
- 도로 도구를 고른 뒤 드래그해 길을 놓습니다. 드래그 중 `Esc` 또는 우클릭으로 미리보기를 취소합니다.
- `Esc`로 선택한 도구를 해제합니다. 공사장 우클릭은 가능한 공사를 취소합니다. 일반 실행 취소(Undo)는 없습니다.
- 마우스 휠로 확대·축소, 드래그로 지도 이동. 도로 도구 사용 중에는 `Space`+드래그 또는 가운데 버튼 드래그로 이동합니다. 방향키/WASD도 지원합니다.
- 하단 속도 버튼으로 일시정지·1배·3배·5배를 선택합니다. `O`로 문제 보기를 켜고 끕니다.

**같은 브라우저·같은 주소에서 저장·이어하기를 지원합니다.** 자동 저장은 최근 3개를 보관합니다. 설정의 `지금 저장`으로 수동 저장하고, 첫 화면의 `이어하기` 또는 설정의 불러오기를 사용하세요.
브라우저 사이트 데이터를 지우면 저장도 사라집니다. `저장 불가`가 표시되면 브라우저 저장소를 쓸 수 없는 상태이므로 탭을 닫기 전에 확인하세요.

- 시간:
- 막힘:
- 지루함:
- 못 찾은 정보:
