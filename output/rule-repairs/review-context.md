# R1-fix 컨텍스트·결정·출처 검토

판정: **PASS — 컨텍스트·권한·provenance 범위.** R-5 공유 용량 해석은 아래와 같이 명시하며 최종 결과 문서에도 남겨야 한다.

검토 범위: 제품 작업트리 `/Users/rexxa/github/feudal-lord-simulator-playable`, `1c09e99..d0db74b2cc8d82f6a90e2239c4e3c76f04e6ca03` 및 검토 당시 미커밋 R-9. 읽기 전용 Git·문서·원본/이식본 바이트 비교만 수행했다. 제품 파일 수정이나 무거운 테스트 실행은 하지 않았다. 최종 17개 시나리오·5개 seed·7개 탐침 실행, 독립 69개 성능 측정, 깨끗한 클론 검증은 별도 관문이며 이 판정에 포함하지 않는다.

## 확인한 요구사항 해석

**CTX-1 (R-5, 해석 정리로 해소)**: `docs/design/rule-repairs/WORK_ORDER.md:39`는 넘친 동안 **그 품목 신규 입고만** 막으라고 한다. 그러나 `docs/design/rule-repairs/S6-cancellation.md:5`, `docs/decisions/README.md:84`는 공유 저장 한도를 그대로 적용한다고 정한다. `src/economy/storage.ts:140-145`의 `availableSpace`는 모든 실물 재고와 예약을 합산하므로 혼합 창고에서 목재 귀환으로 초과하면 석재 등 다른 품목도 입고 공간 0이 된다. `tests/constructionCargoRecovery.test.ts`의 석재200+목재8 사례도 전체 availableSpace=0을 기대한다. 구현은 취소 화물 교착을 해소한다. 품목 한정 문구와 독립적인 기존 공유 용량을 함께 설명해야 한다.

리더가 명시한 해석: 넘침 품목의 입고 제한은 기존 공유 물리 용량을 무효화하지 않는다. 석재200+목재8, 한도200이면 다른 품목도 독립적인 공유 공간 부족으로 입고0이며, 소비로 실제 공간이 생기면 일반 입고가 다시 가능하다. 경제 용량을 바꾸지 않으라는 범위 제약과 일치하므로 이 해석 하에서 기능 누락으로 판정하지 않는다. 다른 품목에 무제한 용량 예외를 만들지 않는다. 원 작업지시서 문구는 보존하고, 최종 보고에 이 해석과 한계를 명시한다.

## 검증한 일치 사항

- **원본 탐침 7/7 출처 일치**: `fixtures/rule-repair-probes/manifest.json`의 원본 SHA와 `/tmp/fls-r1fix-20260924/codex_R1fix/reference/R1/raw/probes/` 7파일의 SHA가 모두 일치. 이식본 SHA도 모두 일치. 원본의 `/tmp/fls-r1/src/`만 `../../src/`로 치환한 바이트가 이식본과 7/7 완전히 같았다. 입력·관측·assertion 변경 없음. `/tmp/fls-r1fix-20260924/before/`에는 현재 S0 본선에서의 일곱 재현 출력이 존재한다. service P1의 기존집 탈락, 인력 B/C/D 소실, food-first 공사0, 석벽 직렬막힘, 취소화물3000틱 잔류, 석재100→70, 목재시설 미확장을 직접 읽었다. 최종 비재현 판정은 별도이다.
- **69개 slow input 출처 일치**: `fixtures/autoplay-search-budget/manifest.json`의 69개에 대해 원본 `output/trunk-baseline/records/*/slow-decision-*-input.json.gz`를 해제한 바이트와 `slow-inputs.tar.xz` 내부 바이트를 각각 SHA256 비교했다. 69/69 모두 manifest와 일치하고 JSON tick도 일치했다. 원본 파일은 71개이며 제외2개는 `48-seed1-142200`, `48-seed1-143280`이다. S0 `slow-decisions.md:5,78-79`에 두 샘플이 다음 advanceTick을 가리켜 결정 내부 스택으로 쓰지 않는다고 이미 명시되어 있다. 이 검토는 **69개 확정 결정 스택 입력**의 provenance만 승인하며, 71개 전부 성능 해소를 검증했다는 표현은 승인하지 않는다.
- **기준선 보존**: 현재 `seeds/baseline-7db9df85.json`과 S0 `1c09e99`의 해당 파일 SHA256이 모두 `364704f008ca1f9e280e7c60365c62a8c19731964caf9b290eb077247ff70df3`로 같다. 24필지·120만틱·안정24,000틱·시설 및 경고 조건을 현재 상태 기록으로 대체하지 않았다. 기준선/성장 종료 및 가드레일 판정 파일에 이번 변경 없음.
- **A⁵-1과 S0의 상태를 과장하지 않음**: `docs/STATUS.md:15-17`, `output/playtest-a-five-one/REPORT.md`, `output/trunk-baseline/REPORT.md`는 A⁵-1 E1~E4 해소와 가드레일2/5(실패1·미판정2), S0의 합격판정 없는 상태기록을 구분한다. 비축 교착 수정 `9093c33`, B8 merge `7968bcf`는 S0의 조상임을 Git으로 확인했다. 기존 reserveDeadlock, 벽 통행, 가격·시대·자원 설정은 R1-fix에서 재작성하지 않았다.
- **이미 있던 석벽 벽 운반**: `709c25d`가 S0의 조상이고 그 당시 `routing.ts`는 palisade와 stone_wall 양쪽에 wallCarryRoute를 적용한다. 현 `routing.ts:259-270`도 동일 분기를 유지한다. S4 명세의 ‘이미 공유’ 주장은 근거가 있다. 새 성벽 병렬 작업 규칙과 기존 운반 경로를 분리했다.
- **R-T17 한계 정직성**: 자연 A⁵-1 seed2 스냅샷 부재와 준비한16구간 대체를 `S6-cancellation.md:11`, `b064330`의 Not-tested에 명시했다. 테스트도 prepared라고 이름붙였다. 자연12구간을 석벽으로 변환한 R-T9는 별도 검사이며 자연 석벽 시대 플레이 증거가 아니다.
- **권한 밖 큰 변경 없음**: diff상 C2 장부/통행세, 판매 품목 UI, 새 건물·자원, 곡선 렌더/성능 계측 도입 없음. content 변경은 R-3의20%상수와 operationPaused 필드뿐이며 world/geometry 변경 없음. R-3의 핵심 목재 사슬 우선은 R-T5의 명시적 벌목·제재 유지 요구를 위한 좁은 변경으로 문서화되어 있다. R-7/R-8은 봇 행동 선택, R-9은 결정론적 후보 예산 및 진단 범위다. R-9 수동 성벽 추천 기본값 Infinity와 후보한도 캐시 키 분리도 확인했다.
- **저장 요구 대응**: R-3은 v3·v2→v3·지문·fixture, R-7은 v4·v3→v4·지문·fixture를 추가한다. 저장 형식 변경 없다는 잘못된 주장은 하지 않는다. 이번 검토는 파일/결정 대응만 확인하며 최종 B8 실행 성공을 대신하지 않는다.

## 최종화 때 남은 문서 작업

1. `docs/STATUS.md:7-21`은 아직 S0 완료/R1-fix 다음 작업을 말한다. R1-fix 종료 시 현재 결과와 미통과/미판정 관문을 갱신해야 한다. 과거 A⁵-1/S0 이력은 유지한다.
2. `docs/decisions/README.md:77-88`에는 R-1~R-8만 있으며 R-9은 미커밋 명세 단계다. R-9 확정 시 색인·저장 불변·성능/결정론 한계를 연결해야 한다.
3. ‘이미 해소(커밋)’ 보고에는 석벽 벽 운반 `709c25d`, A⁵-1 비축 교착 `9093c33` 등을 명확히 연결한다. 전체7항목이 과거에 이미 해소되었다고 확대하지 않는다.
4. 최종 첫 줄은 작업지시서의 ①N/17 · ②N/5 · ③재현N/7 형식을 실제 고정 HEAD 증거로 채운다. 현재 PASS라고 새로 주장한 런타임 결과는 이 리뷰에 없다.

검토에 사용한 일반 메모는 제품 작업트리를 현재 Git으로 재확인하라는 지침뿐이다. 모든 기술적 판정은 이번에 읽은 제품·참조·Git에 근거한다.
