# 유기적인 땅 — 설계 3종 통합안 (2026-09-24)

대상: **Fable 설계안 v1**(`c26f9af`, 원칙·0단계·에셋 규격) · **설계 v3-A**(Claude Code, `c26f9af`+`dd19986`, 탐침 5종으로 수치 근거) · **설계 v3-B**(Codex 세션 `astra-v3`, `cc20def`+`dd19986`, 저장·편집 계약 상세)
원칙: 셋 다 (나) "판정은 격자, 곡선은 파생"으로 수렴. 차이는 **무엇을 저장하느냐**와 **1단계 범위**.

## 1. 세 설계가 일치한 것 → 확정

| 항목 | 내용 |
|---|---|
| 구조 | (나) 격자 판정 + 곡선 표면. 자유 좌표(다) 기각 |
| 도로 그래프 | **4-연결 유지**, 이동 비용 맨해튼(대각 길은 보이는 길이의 약 1.4배 걸음). A 탐침: 8-연결 이득은 정확히 45°에서만 |
| 도로 렌더 | 도로 셀 체인에서 파생한 중심선 + 리본. 교차로·막다른 끝·성문·다리 경계는 **고정점**. 광장(2×2 이상)은 윤곽으로 |
| 성벽 | 논리 원본 = 기존 격자 모서리 경로. 곡선은 표시 전용. 성문 주변 직선 잠금. 벽 운반·공사 구간 무변경 |
| 지형 경계 | 셀에서 marching squares 계열 윤곽, 이웃이 **같은 선 하나를 공유**. 숲·물 의미 무변경 |
| 구역 | 원본 = 그린 형상, 판정 = 셀 소속(셀 중심 기준). frontage 필지는 도로를 따라 파생 |
| 렌더 성능 | Canvas 2D 유지, clip 없는 직접 채움, **지면 청크 래스터 캐시**, 기능 플래그 |
| 에셋 | 표시 크기 기준 제작(Astra 파일럿 교훈), 띠 경작지는 절차 띠 + 상태 재질, 텃밭은 큰 이랑 소품 |

## 2. 갈린 것 → 채택안

| 항목 | Fable | v3-A | v3-B | **채택** | 이유 |
|---|---|---|---|---|---|
| 저장하는 것 | 없음(구역은 3단계) | **`zones[]`만**(v2) | 도로 원본 곡선·소유자·제외 셀·폐기 ID·확정 필지·구역 | **A** | 저장이 늘수록 이주·결정론 부담이 커짐. B의 도로 원본 저장은 획 충실도가 부족할 때 2단계 선택지(A의 Q2)로 |
| 도로 스무딩 | Catmull-Rom, 지그재그 합치기 | **이동평균 5 + Chaikin 2**, 노드 고정 | 정수 flatten + cubic | **A** | 탐침 근거: 셀 중심 직접 스무딩은 계단이 뱀처럼 되살아남, A 방식은 리본 넘침 0 |
| 도로 입력 | L자·대각 드래그(D2) | 곡선 획 → 셀 래스터(스냅 규칙) | 곡선 획 → 정수 래스터 | **단계적: 먼저 D2 L자·대각, 다음 A의 곡선 획** | 획 도구는 입력 의도·스냅·컨트롤러 경유점까지 필요. L자 드래그로 먼저 체감 확보 |
| 성벽 허용 | 0.15칸 | 편차 ≤0.35 + **차단 판정 불일치 0**(lots24 실측) | Hausdorff ≤1/8칸 + 차단 동치 + 운반망 동치 | **차단·운반 동치를 하드 게이트(A·B), 편차 상한은 1단계 측정 후** | 거리만으로는 통행 일치를 보장 못 함(B 지적). A가 0.35에서 불일치 0을 실측 |
| 구역 겹침 | — | 나중 구역 우선 | 같은 층 겹침 거절 | **C1 명세 때 결정**(편집 편의 vs 단순성) | |
| 건물 배치 가능 셀 | — | 소속 − 점유 | **footprint 전체가 구역 안(`interiorPlacementTiles`)** | **B** | 경계에 걸친 건물 방지 |
| 도로 변경 시 기존 집 | — | 필지 파생, 건물은 필지 참조 안 함 | **기존 건물·확정 필지는 움직이지 않음**, 접근 상실만 진단 | **A+B**: 필지는 파생, 기존 건물은 절대 이동·철거 없음 | |
| 평행 근접 도로 | — | — | 의도 없는 인접 접속 거절 | **B** | "보기엔 끊겼는데 그래프는 이어짐" 방지 |
| 꼭짓점 변위 | hash ±0.12칸 | 없음(변 중점 통과) | 없음 | **없음으로 시작** | 무작위 흔들림 금지 원칙과 거리. 필요하면 아트 판단으로 추가 |
| 1단계 범위 | 렌더 전용(경계층·중심선·청크 캐시) | 렌더 + 곡선 도로 도구 + 구역 v2 + ZoneFillAgent v0 | A와 비슷, 32×32 고정 fixture, S01–S20 | **분할**(3절) | 렌더 전용은 B단계와 병렬 가능, 구역·채우기는 R1 선결 과제가 있음 |

## 3. 단계 재구성

| 단계 | 내용 | 선행 | 담당 |
|---|---|---|---|
| **S0** | 본선 정리(B8 병합, 테스트 이식성, 기준선, `TileEdgePoint`) | A⁵ | Codex |
| **D1a** 렌더 전용 | 도로 리본(셀 체인 → 평균5+Chaikin2, 노드 고정), 숲 경계 1곳 윤곽, 밭 무리 외곽선, 지면 청크 캐시, 플래그. 게임 상태·저장 무변경 | S0, B11 계측 | Claude Code (B와 병렬) |
| **D1b** 도로 입력 | L자·대각 드래그(D2) → 이후 곡선 획 도구(A의 R-road·스냅 규칙, `place_road_path{cells}`) | D1a | Codex |
| **R1 선결** | 서비스 배정 규칙(S2-F1), 공사 공급처·처리량(S4-F2·F3) | S0 | Codex (B 도중) |
| **C1** 구역 | `zones[]` v2(B8 틀), 소속·`interiorPlacementTiles`, frontage 필지, ZoneFillAgent v0(필지 ≤10으로 시작), 경작지 띠·텃밭 이랑 | B1·B2, R1 선결 | Codex |
| **D2** | 연결 일반성(교차로·광장·다리·철거) | D1b | — |
| **D3** | 성벽·해안 곡선(차단·운반 동치 테스트를 하드 게이트로) | D2 | — |
| **D4** | 대형 도시 성능 | — | — |

## 4. 결정

| # | 질문 | 결정(권고) |
|---|---|---|
| Q1 | 대각 도로 이동 비용 | **맨해튼 유지**(셋 모두 권고) |
| Q2 | 도로 원본 | **셀**. 획 곡선 저장은 필요 시 D2 이후 |
| Q3 | 성벽 둥글림 | **구속 스무딩**, 동치 게이트 |
| Q10 | B8 먼저 병합 | **예**(S0에 포함) |
| Q11 | 구역 채우기 전 서비스 배정 | **건설 순서 우선**: 먼저 지어진 집이 우선권. `nextConstructionOrdinal`·건물 ID 서수가 이미 상태에 있으므로 **새 저장 필드 없이** 결정론적. 새 집이 옛 집의 서비스를 빼앗지 않음. R1 S2-F1 해소 |
| 성내 경작지 | 시제품은 **성내 신규 경작지 금지**(B 권고), 소규모 예외 기준은 C1에서 |
| frontage 집 방향 | **집은 기존 방향 유지**, 곡선은 앞선·뒤뜰·이랑으로 읽힘. 건물 회전은 별도 연구 |
| 텃밭 저배율 | **0.6배에서도 큰 이랑 윤곽 유지** |

## 5. D1a 구현에서 정한 것 (2026-09-24, 렌더 전용 · 플래그 `RENDER_BOUNDARY_V2`, V1에서 사용자 결정으로 기본 켬)

채택표(2절)는 바꾸지 않았다. 구현하면서 정한 세부와 그 근거만 남긴다. 측정은 [D1a 보고서](../../verification/d1a/REPORT.md).

| 항목 | 정한 것 | 근거 |
|---|---|---|
| 도로 스무딩 | 채택안 그대로(이동평균5 + Chaikin2, 고정점 비스무딩). 추가로 스무딩한 꼭짓점을 자기 칸에서 0.2칸 안으로 끌어당긴다 | 인접 두 칸을 r만큼 키운 합집합은 볼록이라 선분·Chaikin 점이 같은 한계를 물려받음. 실측 도로 칸 밖 최대 0.125칸 |
| 도로 폭·재질 | 보이는 폭 0.55칸(스트립 48행), 석재길 = **완공 성벽 구간이 있는 성곽 다각형 안** 칸, 나머지 흙길. 전환은 칸 경계 ±0.25칸 알파 섞기 | 도로 종류 필드가 없어 성벽 안/밖 임시 규칙(지시서 2항). 도로 종류가 생기면 교체 |
| 광장 | 2×2 도로 덩어리 = 광장 윤곽, 스트립의 불투명 띠를 거울 반복한 면 재질(세계 좌표 정렬) | 스트립을 늘여 쓰면 바퀴 자국이 광장을 가로지름 |
| 숲 윤곽 | 칸 중심 표본 marching squares(꼭짓점 = 칸 변 중점) + Chaikin2, 지도 밖은 숲으로 취급(테두리에 선·데칼 없음) | 45° 계단이 곧은 사선이 됨. 실측 칸 변 최대 0.25칸 |
| 밭 무리 윤곽 | **칸 변 추적(꼭짓점 = 칸 모서리)** + Chaikin2 | 칸 중심 표본은 2×2 밭 하나를 타원으로 깎음(모서리 면적 손실). 변 추적은 직사각형에 0.25칸 둥근 모서리. 실측 최대 0.125칸 |
| 경계 소유 | `boundaryEdgeKey`(변 하나에 키 하나, 양쪽 타일 공통). 숲 데칼 변형·반전·크기는 변 키 해시 | Fable F4. 플래그 끔 경로의 `forestFringeGeometry`(타일+방향 키)는 ⑤(픽셀 동일) 때문에 그대로 둠 |
| 청크 캐시 | 8×8칸 두 층(지면 / 도로 리본), 키 = 청크가 그리는 기본 도형의 해시 + 에셋 준비 비트 + 줌 0.05 단계 + DPR. 화면 청크 + 화면 밖 64개 유지, 시야 둘레 1청크는 유휴 시간에 미리 래스터 | 지시서 5항. 그림자·지형 장식(`townLandscape`)·앞마당·다리 상판은 캐시 밖에서 매 프레임(나무 벌채·주택 등급·그림 로딩을 키에 넣지 않기 위해) |
| 워커 | 셀 중심 경로 그대로. 모퉁이에서 리본 중심선과 셀 중심 사슬 사이 최대 0.625칸 | D1a 범위 밖(지시서에 없음). 워커 중심선 오프셋(Fable A2)은 D1b/D2 후보 |
