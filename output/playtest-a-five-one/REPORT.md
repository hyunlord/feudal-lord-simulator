관문: ①가드레일 2/5(실패 1·미판정 2) · ②E1~E4 4/4

## A⁵-1 가드레일 복구

- 기준: `181d075` → `d974938`; 브랜치 `codex/phase15-organic-ground`.
- 식량: 최근 2,400틱 실측에서 밀 생산/소비 또는 빵 생산/요청이 1.05 미만이면 자동 성장의 증설 후보를 낸다. 운송·일꾼·반복 억제와 방앗간/곡창 상한은 유지한다. 원료가 있는 방앗간의 즉시 처리량이 병목이면 방앗간, 그렇지 않으면 밭을 선택한다 (`autoplayFoodMeasuredDecision.ts`, `balanceConfig.ts`).
- 비축 교착: 벽 공사 `reserve_held`, 2,400틱 가용 목재 증가 없음, 목재 생산 건물 `output_full`, 목재 저장 공간 없음이 함께 있을 때만 `reserve_deadlock`을 파생한다 (`reserveDeadlock.ts`). 공사장과 목표판에 공유 창고 포화 원인과 `공사 우선으로 전환` 버튼을 보인다. 자동 성장은 우선순위를 전환하고, 저장 공간이 막은 자원 생산시설을 더 짓지 않는다. 경제 수치와 A⁵ 벽 운반·도로 규칙은 바꾸지 않았다.
- E1 칼날 균형에서 증설 후보, E2 1.2배 여유에서 증설 없음, E3 seed 3 반례에서 원인 표시·버튼 전환·벽 작업 진전, E4 목재 증가 중 오탐 없음: 모두 통과. E3 입력은 `fixtures/reserve-deadlock-seed3-snapshot.json`; 실제 반례에서 만든 테스트 fixture이며 플레이 증거가 아니다.
- [원인·버튼 캡처](evidence/reserve-deadlock.jpg): 실제 React 목표판을 합성 E3 상태로 렌더한 화면. 버튼 클릭 후 우선순위가 `balanced`→`priority`로 바뀌었고 오류는 없었다. 자연 플레이 캡처는 아니다.

| seed | 최종 소스 `d974938` | 최종 틱·안정 | 시설(밭/방앗간/곡창/시장/교회) | 경고 | 밀 생산/소비 · 빵 생산/요청 |
|---:|---|---|---|---|---|
| 1 | **실패: L4 17/24, 빵 0인 집 4** | 1,200,000 · 0 | 22/7/7/2/2 | 4/78 | 0.980 · 1.042 |
| 2 | 미판정: 시간 상한으로 중단 | 648,000 부근 · 미완료 | — | — | — |
| 3 | 미판정: 탐색 병목으로 중단 | 28,080 부근 · 0 | — | — | — |
| 4 | 통과 | 528,792 · 24,000 | 24/9/4/2/2 | 2/80 | 1.007 · 1.023 |
| 5 | 통과 | 750,070 · 24,000 | 26/8/7/2/2 | 0/84 | 1.015 · 1.021 |

이전 소스 `8c8d5a7`의 탐색 실행은 seed 4(497,506틱)와 seed 5(750,747틱)가 각각 L4 24/24·안정 24,000틱·가드레일 통과였다. **최종 커밋의 5/5 증거로 계산하지 않는다.** seed 1은 승리 조건만 670,470틱에 달성했고, 1,200,000틱에서도 밀 생산 982/소비 1002, 빵 생산 501/요청 481, 빵 재고 0인 집 4채였다. 자동 성장 진단은 `wheat_transport_blocked`인데 조치가 `none`이라 식량 여유 기준만으로 운송·배급 불안정을 해소하지 못했다. seed 2는 L4 24/24·인구 768이었지만 석벽 공사 16곳이 남은 648,000틱 부근에서 시간 상한 검증을 위해 중단했다. seed 3은 한 번의 교회 서비스 후보 탐색이 28,080틱에서 약 40분 지속되어 중단했다([샘플](evidence/seed3-service-search.txt)). seed 2·3의 미판정은 시뮬레이션 실패의 증거가 아니다. 표의 생산/소비 비율은 완료 결과의 최근 2,400틱 관측값이다.

필수 조건: 코드 커밋 `d974938`에서 전체 회귀 **2,304/2,304**(Phase 9 포함), typecheck, build 통과. 보고서 커밋에서 전체 회귀·typecheck·build를 다시 실행한다. 빌드의 기존 500KB 청크 권장 경고는 남는다. E1~E4 관련 테스트 38/38, `git diff --check 181d075..d974938` 통과. 원격 해시와 로컬 해시는 최종 보고 시 재확인한다.

다음 후보: seed 1의 `wheat_transport_blocked`와 빵 재고 0인 집 4채 사이의 배송·배급 원인을 추적하고, seed 2의 석벽 공사 16곳 정체, seed 3의 교회 서비스 후보 탐색 비용을 별도로 진단한다. 기준선이나 안정 조건을 완화하지 않는다.

소요 시간: 2026-09-24 05:51 UTC 시작, 08:12 UTC 판정 고정(약 2시간 21분). 배포·main 병합·PR 없음.
