const fs=require('fs'),path=require('path'),crypto=require('crypto');
const sharp=require('/Users/rexxa/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/sharp');
const {createCanvas,loadImage}=require('/tmp/astra-wave4b-work-20260925/node_modules/@napi-rs/canvas');
const root=path.resolve(__dirname,'..'), mp=path.join(root,'records/metadata-scenes-build.json');
(async()=>{const rows=JSON.parse(fs.readFileSync(mp)); const canvas=createCanvas(720,8*112+40),ctx=canvas.getContext('2d');ctx.fillStyle='#e9dfc9';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#34271b';ctx.font='16px sans-serif';ctx.fillText('24 / 32 / 48 / 96 px',320,24);
for(let i=0;i<rows.length;i++){const r=rows[i],g=r.generationRecords.at(-1),raw=path.join(root,g.rawFile);if(!fs.existsSync(raw))fs.copyFileSync(g.generatedPath,raw);const {data,info}=await sharp(raw).ensureAlpha().raw().toBuffer({resolveWithObject:true});let l=info.width,t=info.height,rr=-1,b=-1;for(let y=0;y<info.height;y++)for(let x=0;x<info.width;x++)if(data[(y*info.width+x)*4+3]>4){l=Math.min(l,x);rr=Math.max(rr,x);t=Math.min(t,y);b=Math.max(b,y)};const crop={left:l,top:t,width:rr-l+1,height:b-t+1};const fit=await sharp(raw).extract(crop).resize(84,84,{fit:'inside'}).png().toBuffer();const m=await sharp(fit).metadata();await sharp({create:{width:96,height:96,channels:4,background:'#00000000'}}).composite([{input:fit,left:Math.floor((96-m.width)/2),top:Math.floor((96-m.height)/2)}]).png().toFile(path.join(root,r.file));const final=await sharp(path.join(root,r.file)).metadata();g.rawWidth=info.width;g.rawHeight=info.height;r.alphaCrop=crop;r.sha256=crypto.createHash('sha256').update(fs.readFileSync(path.join(root,r.file))).digest('hex');r.qa={...r.qa,dimensions:final.width===96&&final.height===96,channels:final.channels};ctx.fillStyle='#34271b';ctx.fillText(r.id,10,76+i*112);const im=await loadImage(path.join(root,r.file));for(const [j,s] of [24,32,48,96].entries())ctx.drawImage(im,320+j*90,40+i*112+(96-s)/2,s,s);}
fs.writeFileSync(mp,JSON.stringify(rows,null,2)+'\n');fs.writeFileSync(path.join(root,'records/scenes-build-size-check.png'),canvas.toBuffer('image/png'));console.log('8 assets processed');})();
