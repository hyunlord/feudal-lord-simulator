관문: 판정 대기 — 시나리오 M 4/4 · 전체 회귀 `b425d46` 3,091/3,091 · seed 2·4 미도달 필지 1,722·2,702 → 0 · 가드레일 1회차 5/5(이전 코드), 2회차 4/5 — seed 3 14필지 정체(마지막 수정 전) — 실행 2회를 다 써서 마지막 코드는 가드레일 미확인, 본선에 합치지 않음

# MARKET-1 시장 도달 범위 보고서

지시서: MARKET-1(첨부 두 번째 문서 뒷부분). 명세: [시장 도달 범위 명세](../../design/market-reach.md) MK-1~MK-4. 결정: MK1~MK4. 엔진 세션, 렌더 0줄, 검증 DGX. 브랜치 `claude/market1-reach`(원격 푸시, 본선 미병합).

## 만든 것
- **도달 범위(MK-1)**: 시장은 길로 40걸음 안의 집을 받는다.
  - 보정: 예전 "건물 거리 8 ∧ 길"이 받던 집을 모두 받는 최소 걸음이다. 가드레일 도시 13~38, F0-A seed 4는 40이다.
  - 가까운데 길이 없는 시장은 `unreachable`, 멈춘 시장은 `paused`로 진단 층을 지킨다. 우물·교회는 반경 그대로다.
- **수 상한 폐지(MK-2)**: 첫 시장은 늘 된다. 그다음은 받지 못하는 필지(범위 밖·수용 참)가 12 이상일 때만이다. 봇 서비스 계획·AR-2·AR-5와 가드레일 수용(1 + ⌊필지 ÷ 12⌋)이 지킨다.
- **예측 API(MK-3)**: `marketReach`(길 칸·받을 집)·`marketRoadDistance`·`anotherMarketAllowed`·`marketUnservedLots`.
- **봇(MK-4)**:
  - 아직 없는 길 위의 증명(서비스 공간·합동 예산·길 계획)은 반경을 대리 조건으로 쓴다(`marketConnectionOnly`).
  - 상한 시 새 집은 길 도달 안에 둔다. 길이 아직 없는 자리는 반경을 대리로 쓴다.

## 관문 결과
- **시나리오 M1~M4 4/4.**
- **seed 2·4 미도달 필지 감소**(같은 도시를 두 규칙으로, `e6d61b6`, [market-seed2.json](market-seed2.json) · [market-seed4.json](market-seed4.json)): 계절 × 미도달 필지가 seed 2는 옛 반경 1,722 → 길 도달 **0**, seed 4는 2,702 → **0**이다. 두 도시 모두 시장 하나다(기준선 둘).
- **가드레일**([guardrails.json](guardrails.json)):
  - 1회차(`b5a2c48`)는 5/5다. 다만 seed 3이 72,000~120,000틱에 9필지에서 멈췄다. 봇 증명이 지어질 길에서 40걸음을 재 첫 시장 자리를 증명하지 못했다 → 증명을 반경 대리로 고쳤다.
  - 2회차(`e6d61b6`)에서는 seed 3이 336,000틱에 14필지 · L4 14로 멈췄다. 상한 시 새 집 자리를 길 도달로만 보니, 길이 아직 없는 성 안 자리가 모두 거절됐다(시험 B7과 같은 모양) → `b425d46`에서 길 없는 자리는 반경 대리로 고쳤다. B7이 통과한다.
  - 2회차 최종 **4/5**: seed 1·2·4·5는 24필지 · L4 24/24(승리 174,294 · 165,897 · 180,192 · 196,285, 끝 290,606 · 224,850 · 268,236 · 467,993, 시장 하나씩), seed 3은 1,200,000틱까지 14필지 · L4 13(위 원인, `b425d46`에서 고침). 승리 틱은 기록만이다(기준선 cbc84d0 대비 +21 % · −3 % · −0.4 % · −30 %).
- **전체 회귀**: 마지막 커밋 `b425d46` **3,091/3,091** · typecheck 통과.
  - 옛 반경·옛 상한을 적은 시험들을 새 규칙으로 다시 적었다: 시장 공백 B3·B5(이제 길로 받음), 투입 과잉 시장 4개, 교체 시장, 집 진단, 옮기기가 앞서는 옛 도시 셋.
  - 고정값(seed 3 경로 캐시, L-10 seed1City)을 다시 적었다.

## 판정이 필요한 것
- 작업당 가드레일 2회를 다 써서, 마지막 수정(`b425d46`)은 가드레일로 확인하지 못했다. **3회차를 돌려 5/5면 합칠지**, 여기서 멈출지 정해 주세요. PERSON-0에서 이미 한 번 한도를 넘겼다.
- 시장 상한 12필지 규칙에서는, 길로 먼 집이 12필지 미만이면 봇이 그 집을 헐어 도달 안에 다시 짓는다(AR-5). 예전 규칙으로 자란 도시에서 이 옮기기가 먼저 나온다. 이 동작을 그대로 둘지 판정이 필요하다.

## 렌더에 넘길 것
범위 원 대신 `marketReach`의 길 칸을 칠하기, "거리 d / 범위 8"을 "길 d걸음 / 40"으로(`serviceDiagnosisModel`·`placementPrediction`·`houseDiagnosisModel`).

## 소요 시간
03:49 시작 → 04:45(약 1시간, 상한 1.5시간 안).
