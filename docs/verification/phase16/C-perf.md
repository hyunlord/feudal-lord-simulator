## Phase 16 — C 도로 연결 라벨
- 커밋: 6c71e7b (4파일), 8cb030c (2파일).
- 바뀐 것: 집마다 도로를 다시 탐색하던 판정을 공유 연결 라벨로 바꿨다. 도로·벽·문·격자크기 변경 시 다시 계산한다.
- 측정: Node25.8.2, 같은 인구176·392,808틱 도시,40표본×4회 중 첫 회 제외. 조용한 호스트에서 순차 실행한 엔진ms이며 FPS가 아니다.

| 작업 | 이전 중앙값/p95 | 이후 중앙값/p95 |
| --- | --- | --- |
| 전체 틱, 통합 | .2205 / .5897 | .2276 / .8065 |
| 전체 틱, C만 | .2205 / .5897 | .2213 / .6795 |
| 주택8채 연결 질의, 통합 | .2272 / .6147 | .0080 / .0144 |

- 테스트: 무작위200도시 동등성·격자 재해석 회귀, 최종1512/1512·타입·빌드 통과. C만 적용하면 모든 실행의 최종 상태 SHA가 같다. 양쪽 연결 질의는 매회320개 도달 판정으로 같다.
- 확인 못 한 것: 전체 틱 속도 개선은 확인되지 않았다. 기준 도시는8가구가 시장 범위 밖이라 일반 서비스가 도로 질의 전 제외한다. 별도 연결 측정은 거리 조건을 건너뛴다. 통합 상태 차이는 승인된 D의 prosperityTicks1200→0 하나이며 기존 승리 기록은 유지된다.
- 사용자 결정 필요: 없음.

재현: `mkdir -p output/phase16` 후 `gzip -dc tests/fixtures/phase16-city.json.gz > output/phase16/canonical-city.json`; `node --import tsx scripts/phase16RoadBenchmark.mjs <비교할 작업트리> output/phase16/canonical-city.json tick` 또는 마지막 인자 `service`. 원본 b8ed650 / C만94a2e4c / 통합5e4f6bb, Node 버전과 도시 SHA는 render 요약과 동일하다.
