관문: ① 결산 카드 3칸 = 그 계절 원장의 심각도 상위 기록(Wave 19 아이콘) · ② seed 2 1장의 여섯 계절 카드가 모두 뜨고 아이콘 24/24가 실린다 · ③ 면적 예산 유지 · ④ 깨끗한 클론 — 통과. ⑤ 사용자 판정 대기

# UI-4b 계절 결산 세 장면을 원장에서 (Wave 19)

- **범위:** UI·스타일·에셋 설치·대장·스크립트·테스트·문서만 바꿨다. 엔진·state·save·content·zones·population은 0줄이다. 원장은 `history.query`로 읽기만 한다.
- **검증:** DGX(`scripts/ui4bVerification.sh`)에서 돌렸다. 기준은 본선 `b74a97af`(UI-4 병합 뒤)다.
- **명세:** [UI-4 명세](../../design/events-decisions-chronicle-ui.md) UI4-8, 결정 UI4-D7·D8.

## 관문
| 관문 | 결과 | 증빙 |
|---|---|---|
| ① 원장에서 세 장면 | 닫힌 계절의 기록(심각도 1 이상)을 무거운 것부터 24개 장면 아이콘으로 옮기고, 한 아이콘에 한 칸을 준다. 조용한 계절은 계절 숫자로 채운다. 단위 테스트 5개: 대기근·첫 시장·불탄 집 셋 → "대기근 · 화재 3채 · 장날 성황", 조용한 계절 → 숫자 순, 템플릿별 대응, 두 해 뒤 카드 뜸, Wave 19 설치. | `tests/seasonLedgerScenes.test.ts` |
| ② 실제 1장 | seed 2 1장 봇 실행(`scripts/ui4bSeasonStates.ts`)에서 계절이 닫히기 30틱 전 상태 여섯을 떴다. 1배속으로 열면 여섯 모두 카드가 뜨고, 세 아이콘이 모두 실렸다(96×96 해독). | `captures/*.jpg`, `captures/ui4b-captures.json` |
| ③ 면적 | UX-3 면적 24칸 모두 예산 안이다. | `hud-coverage.json` |
| ④ 클론 | DGX `npm run remote:clone-check`, 커밋 `4306015`: npm ci·타입 검사·테스트 3,053/3,053·빌드 통과. | — |
| ⑤ 사용자 판정 | 대기 | — |

여섯 카드(틱은 저장 시점, 카드는 30틱 뒤 계절이 닫힐 때 뜸):

| 계절 | 세 장면 |
|---|---|
| 첫 건물(970) | 인구 늘음 · 시설 완공 · 목재 부족 −68 |
| 조용한 계절(8,970) | 인구 늘음 +9% · 빵 부족 −30(셋째 칸은 비어 있음: 원장과 숫자에 더 없음) |
| 첫 화재(9,970) | 화재 1채 · 주택 완공 · 인구 늘음 +14% |
| 청원(30,970) | 특허 · 인구 늘음 +10 · 비축 충분 +27 |
| 시장도시(63,970) | 특허 · 인구 줄음 −3 · 목재 부족 −18 |
| 대기근(67,970) | 대기근 · 장날 성황 · 비축 충분 |

## 만든 것
- **받기와 설치:**
  - 받기: 작업을 시작할 때 Wave 19는 `assets-inbox/`에 없었다(원격 브랜치 14개, 로컬 체크아웃, `astra-raw`). 그래서 이 작업이 따로 받아 두었다(받은 바이트, SHA256SUMS 53/53).
  - INBOX-1i: 병합 직전 본선에 INBOX-1i(`e31b0887`)가 들어왔다. Wave 19를 `wave19/candidates-v1`로 받았고, 53장이 확정이다. 따로 받아 둔 사본과 그 장부 57행은 지우고 INBOX 묶음을 쓴다(같은 바이트, inbox 장부 행 = PNG 2,867).
  - 설치: `scripts/installWave19.py`가 확정 53장을 `public/assets/wave19/<묶음>`에 넣고 대장 53행을 만든다. inbox `installed_by`는 UI-4b다.
- **장면:** `src/ui/seasonLedgerScenes.ts`가 원장에서 장면을 고른다. 결산 카드(`SeasonLedgerCard.tsx`)는 30px 아이콘과 값을 보이고, 제목 아래 한 줄에 이름을 적는다.
- **등록만:** 기록 카드 틀 6, 타임라인 14, 전기·세력·지도·관계 9는 `src/ui/wave19ArtManifest.generated.ts`에 올렸다. 9-slice inset과 최소 크기가 들어 있고, CHRON-1에서 쓴다.

## 과정에서 바로잡은 것
- **결산 카드가 두 해 뒤 다시 뜨지 않았다(UI-3부터):** 카드는 닫힌 계절 수가 하나 늘 때 떴다. 그런데 엔진은 닫힌 계절을 8개만 두므로, 9번째 계절부터는 수가 늘지 않아 다시 뜨지 않았다. 증빙 상태 여섯 가운데 첫 계절(970)만 카드가 떴던 것으로 찾았다. 이제는 마지막 닫힌 계절의 끝 틱을 본다(`seasonJustClosed`). 불러오기처럼 여러 계절을 건너뛰면 여전히 띄우지 않는다.
- **화재 칸의 채 수:** 화재 사건 기록이 앞서면 채 수가 빠졌다. 그 계절의 불탄 집 기록 수를 붙였다.

## 판정을 받을 선택지
- **조용한 계절의 빈 셋째 칸:** 원장과 숫자에 변화가 둘뿐이면 칸이 비어 있다. "큰 일 없음" 같은 칸을 둘지는 판정 몫이다.
- **"대기근"·"특허" 아이콘:** 24px에서 문맥 없이 읽히는지는 Astra 검수표도 판정을 미뤘다. 제목 아래 이름 줄이 그 몫을 한다.
