/** Settlement status lines added after the first guidance copy (the older lines stay in `settlementGuidanceModel`). */
export const SETTLEMENT_GUIDANCE_COPY = {
  /** FIX-1: stored bread and wheat last less than one season at the current consumption. */
  foodReserveShort: "식량 비축 부족",
} as const;
