## Phase 16 — 최종 통합 검증
- 커밋: 실행 소스 5e4f6bb. 코드·원본·증빙은 별도 원자 커밋으로 보존한다. 4e93a5b만 후보24개 이동과 생성기/테스트4개 경로 변경을 함께 기록한 이관 예외다. 과거 이력은 재작성하지 않았다.
- 바뀐 것: 주택 래스터·경량 이미지·도로 연결 라벨·이동 대응 성벽 캐시, L4 승리 조건, 관리 오버레이, 가려진 주택 선택, 빈 필지 경관을 통합했다. L3 v2를 보존하고 새 L3/L4 아트는 비교 후보로만 남겼다.
- 측정: dist18,665,422bytes. 자연 seed1 400,129틱 L4기준 충족→401,328틱 승리→427,877틱까지 최종24,000틱 안정. A/C/E 성능 표는 해당 보고서에 기록한다.
- 테스트: TypeScript PASS / 전체1512/1512(18 suites, Phase9 포함, 실패·취소·생략0,521.273초) / build PASS. HTTP73종 크기·SHA 일치. H 실제 초기 플레이 생활L3→L1/건축L3→L3, I8조합 뒤집 선택, F2도로 설치/복원, E36조합 캐시 반복·복귀 픽셀동일 확인. 독립 최종 QA는 H 오버레이 그리기3→0과 가림240조합 추가 검증.
- 확인 못 한 것: 596.68KB JS 청크 경고가 남는다. 일반 기기의60FPS는 주장하지 않는다. 성벽 격자 계단·고정 방향PNG·단일 자연 seed 한계는 남는다. 새 경관은 넓은 통제 배치로 검사했고 기존 자연 저장 도시에는 안전한 배치가 없어0개다.
- 사용자 결정 필요: 없음. main 병합·Pages 배포 없음. 증빙은3MB 미만·모든 이미지 링크 유효·이미지 최대1600px를 확인했다. 원격 동기화는 이 증빙 커밋 생성 후 확인하여 최종 응답에 기록한다.

검증 가설은 (1)도로/문 변경 뒤 캐시가 오래된 연결을 유지하는지, (2)축소 본체와 상태 레이어가 좌표를 두 번 축소하는지, (3)주거 조건 전에 승리하거나 앞 건물 선택 처리가 전역 윤곽 모드를 바꾸는지였다. 각 회귀·실제 UI·자연 엔진으로 확인했다. 초기 QA 도구의 축 인자와 카메라 리사이즈 문제는 수정 후 재검사했으며 이전 결과를 최종 증거로 쓰지 않는다.

| 항목 | 완료 기준 판정 | 근거 / 남은 차이 |
| --- | --- | --- |
| A 주택 래스터 | 성능 기준 미충족 | 코드·가독성·폴백 통과; 정지 중앙값 DPR1 49.9→49.9ms, DPR2 83.3→83.3ms |
| B 용량 | 충족 | dist18,665,422bytes, 73개 HTTP 크기·SHA 일치 |
| C 도로 라벨 | 동등성 충족·측정 완료 | 연결8질의 .2272→.0080ms; 전체 틱 .2205→.2276ms로 속도 향상 주장 없음 |
| D 승리 | 충족 | 자연1,200틱 연속 조건·후속24,000틱 안정 |
| E 이동 캐시 | 충족 | 이동/정지 중앙값 비율 두DPR 1.00, 이동 중 캐시 재생성0 |
| F1 성벽 형태 | 부분 완료 | 대각 재질 개선; 점유와 어긋나는 임의 대각 단축 미적용, 격자 계단 유지 |
| F2 경관 | 충족 | 점유 없는 드문 장식·실제 UI 설치/제거; 통제한 넓은 배치 검사 |
| F3 주택 | 후보 제작 완료 | 비교만 제공, 승인 L3 v2 유지, 새 후보 미설치 |
| H 상태 | 충족 | 실제 퇴화/복구·등록 그림 출력/제거 |
| I 선택 | 충족 | 두 합필축·DPR/줌8조합, 전역O·건설도구 보존 |
| G 정리 | 로컬 충족 | 이력 유지·후보 보관·좁은LFS; 원격 업로드 검증은 푸시 뒤 |
