- tests/autoplayMaterialPriorityPortable.test.ts — "Given a qualified material expansion and a waterless home When public autoplay decides Then executable food construction wins without spending the material episode": asserted the advisor builds `wheat_farm` (a plain building, buildable on any terrain) ahead of a material episode; the fixture town (materialPolicyTown/observedMaterialTown) has only one open, non-rock tile, so no 2x2 arable block or farmstead can ever be placed there under AF-13 grain-on-farmland, making the food branch structurally unreachable in this fixture (falls through to `well` instead) (AF-13)
- tests/autoplayFoodMissedMeals.test.ts — "a common reachable raw reserve too small for the measured shortfall cannot authorize a mill": tested the old `bufferedMillDeficit` wheat-margin backstop (a small common wheat reserve blocking mill authorization even when mills are otherwise unstarved/supplied); AF-13 removed that check, so with mills supplied and not recently starved the decision is unconditionally `mill`/`actual_bread_deficit` regardless of common reserve size (AF-13).
- tests/autoplayFoodLateOutcome.test.ts — "the natural seed 1 strict deficit creates a legal farm through the full advisor and reducer": asserted the full autoplay driver places a new `wheat_farm` construction site; wheat_farm is retired (canPlaceBuilding -> locked_era, isRetiredBuildingKind) and autoplay never builds it (AF-13).
- tests/autoplayFoodLateOutcome.test.ts — "strict-deficit farm release stays closed for blocked-transport": relied on a full wheat_farm barn (old 20-capacity) plus starved mills to make measuredFoodDecision report `wheat_transport_blocked` instead of `actual_wheat_deficit`, releasing the legacy-observation escape hatch differently. measuredFoodDecision's `facilities` filter no longer includes `wheat_farm` (only farmstead/mill/granary), so a wheat_farm-only state (no farmstead) is unconditionally grain-short and the reason is always `actual_wheat_deficit`; the old farm-fullness/backpressure distinction this case exercised is gone (AF-13).
- tests/autoplayFoodLateOutcome.test.ts — "late farm release stays closed for no-current-deficit": same removed distinction as above (wheat_farm inventory no longer feeds measuredFoodDecision) (AF-13).
- tests/autoplayFoodLateOutcome.test.ts — "strict-deficit farm release stays closed for understaffed": relied on the observed wheat_farm building's own `workers` count starving measuredFoodDecision's staff-shortage check; wheat_farm is excluded from measuredFoodDecision's facilities filter now, so its staffing no longer affects the decision at all (AF-13).
- tests/autoplayFoodNetFlow.test.ts — "Given gross wheat surplus but actual market exports When recovering hunger Then choose a farm instead of another mill": removed wheat-flow export-margin deficit rule (measuredFoodDecision no longer reads measured wheat flow or exports; grain is judged solely by the expected harvest / arableSupplyShort) (AF-13).
- tests/autoplayFoodBackpressure.test.ts — "Given {0,2,2,4} spare workers When recovery needs additional staff Then action is {none,none,mill,wheat_farm}" (4 parametrized cases): pinned the old measured-wheat-flow bootstrap ladder (varying measured wheat 20 vs 200 to flip between "needs another farm" and "sufficient", plus idle-worker staffing gating which slot gets built). Grain is no longer judged by measured wheat flow, and with no farmstead built yet the grain slot's own staffing check is skipped entirely (`!completeChain || staffCheck` short-circuits), so idle-worker count no longer gates the first farmstead/field bootstrap (AF-13).
- tests/autoplayFoodBackpressure.test.ts — "Given future civic staff already committed When current idle workers look sufficient Then do not spend the same workers twice": pinned the old wheat_farm bootstrap's staffing gate; with no farmstead built yet the grain slot's staffing check is skipped entirely, so committed-but-unbuilt civic staff no longer blocks the first farmstead/field bootstrap (AF-13).
- tests/onboardingWorldGuidance.test.ts — "second farm guidance has a real construction material route": asserted a world guidance marker for a *second* wheat_farm placement (task-5 expansion). AF-13 retired the wheat farm and made task-5's field expansion a hint-only step (painting more arable cells, `arableCellCount >= ONBOARDING_ARABLE_CELLS`); `onboardingWorldGuidanceTargets` never emits a second farmstead/wheat_farm marker once the first farmstead+mill are built, so this test's premise no longer exists.
- tests/onboardingWorldGuidance.test.ts — "second farm marker skips a nearer disconnected road island": same removed rule — tested the old "second wheat_farm" placement-marker routing heuristic (favoring a connected road island), which no longer exists since task-5 expansion has no world marker at all under AF-13.
- tests/onboardingWorldGuidance.test.ts — "one finished and one pending farm hold expansion without a third farm marker": asserted guidance never offers a *third* farm marker while a second farm is pending. AF-13 removed the "second/third farm" marker concept entirely (task-5 expansion is painted field cells, never a farmstead/wheat_farm world marker), making the assertion vacuously true for the wrong reason and the scenario itself obsolete.
- tests/autoplayMillReplenishment.test.ts — "natural seed 2 retries a none decision after actual mill replenishment before the next120tick pulse": relied on this natural snapshot being a fully-settled town with zero pending civic actions (`driver.apply(start) === start`). Migrating the fixture (needed so measuredFoodDecision recognizes the food chain at all — see AF-13) releases the labour that used to run 22 wheat farms (now 6 farmsteads need only a fraction of it), which unlocks other, unrelated civic construction (e.g. a church) that the old wheat-farm-era snapshot's baseline never had to compete with; the exact "nothing to do" moment this fixture captured no longer reproduces post-migration (AF-13/AF-12).
- tests/autoplayMillReplenishment.test.ts — "the UI none cache refreshes for the same natural replenishment transition": same root cause as above (depends on the same broken "baseline decision is none" premise) (AF-13/AF-12).
